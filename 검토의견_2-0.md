# 2-0단계 지시서 검토의견

> 대상 문서 : `stt_spec_phase2_0.md`
> 검토 : Claude Code · 2026-08-14
> 판정 : 감리 · 2026-08-14
> 성격 : **반박과 그 판정을 함께 남긴다.** 왜 지시서가 지금 모습이 됐는지 다음 사람이 알아야 한다.

---

## 0. 요약

지시서 항목 20건 중 **13건은 그대로 수용**, **7건은 반박**했다. 반박 7건은 모두 받아들여져 지시서가 고쳐졌다.

반박의 근거는 대부분 **실측**이다. 코드를 읽어 짐작한 것이 아니라 이 PC에서 돌려 확인했다.

---

## 1. 실측으로 확인한 사실 넷

이 넷이 반박 1·2의 근거다. 측정 기기는 **RTX 3060 · CUDA · Windows 10 · Python 3.13.7 · ctranslate2 4.8.1**이다.

### 1-1. 모델 생성만으로는 CUDA 라이브러리를 부르지 않는다

프로세스에 적재된 모듈을 `EnumProcessModules` 로 세어 확인했다.

| 시점 | CUDA 관련 모듈 |
|---|---|
| `WhisperModel(device="cuda")` 생성 직후 | **0개** |
| 무음 1초 추론 후 | **0개** (인코더가 돌지 않는다) |
| 잡음 2초 추론 후 | **0개** |
| 실제 음성 26.7초 전사 후 | **20개** |

`cublas`·`cudnn`·`cufft`·`nvrtc` 는 **실제 음성을 인코딩할 때** 비로소 적재된다.

### 1-2. 새 PC의 오류는 전사 중에 났다

보고된 문구가 증거다.

```
전사 중 멈췄다. Library cublas64_12.dll is not found or cannot be loaded
```

`app.py` 에서 `전사 중 멈췄다` 는 **세그먼트 순회 루프의 예외 핸들러**다. 모델 생성 실패는 `모델을 불러오지 못했다` 로 나온다. 문구가 다르므로 실패 지점이 확정된다.

### 1-3. 이 PC는 `nvidia` 휠이 없다. DLL 은 `torch/lib` 에서 온다

```
import nvidia           →  ImportError (네임스페이스 패키지 자체가 없다)
CUDA_PATH · CUDA_HOME   →  없다
PATH 안 cublas64_12.dll →  없다
실제 적재 경로          →  site-packages\torch\lib\cublas64_12.dll  (100MB)
```

적재된 20개 중 18개가 `torch/lib` 이다. `torch` 가 자기 `__init__` 에서 `os.add_dll_directory` 를 부르기 때문에 찾아진다 — **PATH 를 거치지 않는다.**

### 1-4. `import faster_whisper` 가 `torch` 를 끌어온다

```
시작                     torch in sys.modules  False
faster_whisper 임포트 후                       True
```

즉 **이 PC 는 우연히 돌고 있었다.** `app.py` 는 `torch` 를 명시적으로 임포트하지 않는데, `faster_whisper` 의 부작용이 DLL 경로를 잡아 준 것이다.

---

## 2. 수용한 것 — 13건

| 항목 | 확인 |
|---|---|
| §2-2 `finish_job()` | `fail()` 이 `phase`·`eta` 를 지우지 않는다. 실제 결함 |
| §2-3 버튼을 상태에 묶기 | `error` 에서 `#live` 가 계속 보이고 중단 버튼이 남는다 |
| §2-4 오류에 파일명 | 맞다 |
| §2-5 막대 100% | 26.2 / 26.73 = 98.0%. 재현됨 |
| §2-6 종료 후 남은 시간·경과 | 맞다 |
| §2-7 실패가 다음 작업을 가리지 않게 | 진단이 정확하다 |
| §2-9 종료 후 버튼 제거 | 맞다 |
| §3-5 캐시 키에 실제 기기 | **실제 버그.** `auto` 로 남으면 항목마다 CUDA 를 재시도한다 |
| §3-6 `JOB.device`·`device_note` | 맞다 |
| §4-4 이력에 기기 | 맞다 |
| §5-6 파이썬 3.13+ 주의 | 값싸고 유용하다 |
| §6-2 기기 다르면 배속 비교 안 함 | `baseline.json` 에 `device_actual` 이 이미 있다 |
| §6-3 문서 동시 갱신 | 맞다 |

§2-5 의 원인 설명 한 군데만 틀렸다. "마지막 발화의 **시작** 시각" 이 아니라 `seg.end` 다. 결론은 같다.

---

## 3. 반박한 것 — 7건. 모두 수용됐다

### 반박 1 · §3-3 §4-1 — 탐지 지점이 틀렸다

**지시서 안** — `WhisperModel(...)` 생성을 `try` 로 감싸 CUDA 실패를 잡고, 머리말 배지는 `small` 모델을 만들어 판정한다.

**반박** — §1-1·§1-2 로 성립하지 않는다. 그 오류는 생성이 아니라 전사에서 난다. 이 설계로는 새 PC 의 사고를 **못 잡는다.** 더 나쁜 것은 배지 쪽이다. CUDA 가 깨진 PC 에서도 모델 생성은 성공하므로 `GPU 사용 가능` 이라는 **거짓 안심**을 준다.

**판정 — 수용.** 대체 경로를 전사 루프까지 감싸고, 탐침은 실제 음성 추론으로 바꾼다.

### 반박 2 · §3-2 — `nvidia` 휠만 훑으면 범위가 좁다

**지시서 안** — `nvidia.__path__` 를 순회해 `*/bin` 을 DLL 경로로 등록한다.

**반박** — 순회 자체는 옳다(`__file__` 이 `None` 이라는 지적도 맞다). 그러나 §1-3 처럼 `torch/lib` 에서 DLL 을 얻는 PC 는 구제되지 않는다. 그리고 §1-4 대로 지금은 우연에 기대고 있다.

**판정 — 수용.** `torch/lib` 을 후보에 넣고, CUDA 요청 시 모델 생성 전에 `import torch` 를 명시적으로 한 번 한다.

### 반박 3 · §4-1 — 기동 시 `small` 모델 판정은 안 된다

**반박** — 모델이 없으면 **기동할 때 460MB 를 받는다.** 무창 실행에서는 사용자가 이유도 모른 채 기다린다.

**판정 — 수용.** `ctranslate2.get_cuda_device_count()` 로 즉답한다(실측 즉시 `1` 반환). 배지는 기동 시 물리 GPU 유무까지만, 실제 사용 가능 여부는 첫 작업 결과로 확정하거나 진단 화면에서 누를 때 실측한다.

### 반박 4 · §2-8 전체 중지 의미 변경 — 반대

**지시서 안** — 확인 창 → 현재 작업 중단 → 대기열 전부 제거 → **부분 산출물 삭제.**

**사실 정정이 먼저다.** "눌러도 반응이 없다" 는 정확하지 않았다. 버튼 라벨은 `대기열 재개` 로 바뀌고 색이 채워진다(코드 확인). 바뀌지 않는 것은 현재 작업이고, 그것은 원 지시서 §3-1 의 정의 그대로다 — "현재 항목을 끝내고 멈춘다".

> 감리 기록 — 최박사의 관찰을 검증 없이 결함으로 옮긴 것이다. 버튼은 동작했고 화면에 변화가 있었다.

**반박 이유 셋**

- 대기열 전부 제거는 되돌릴 수 없다. 지금은 재개가 있다
- **부분 산출물 삭제가 원칙을 뒤집는다.** 이 프로젝트의 안전장치 전부가 "중단해도 여기까지는 남는다" 를 향해 있다 — 구간마다 `flush`, 2단계 분리, `interrupted` 보존, `_부분` 파일. 전체 중지만 예외로 지우면 3시간 작업이 오조작 한 번에 사라진다
- 1단계에서 이 의미로 인수됐다

**판정 — 수용. 다만 요구 자체는 실재하므로 셋으로 나눈다.**

| 버튼 | 위치 | 동작 | 산출물 |
|---|---|---|---|
| 현재 중단 | 진행 구역 | 지금 것만. 다음 항목 이어짐 | 보존 |
| 전체 중지 | 진행 구역 | 지금 것 끝내고 멈춤 → `대기열 재개` 로 토글 | 보존 |
| **대기열 비우기** | **대기열 구역** | 확인 후 대기 항목 제거 | 보존 |

비우기는 대기열 구역에 둔다. 진행 구역이 아니라 거기가 제자리다. 그리고 전체 중지를 누를 때 결과를 말로 알린다.

```
지금 것을 끝내면 멈춘다. 남은 3건은 그대로 있다.
즉시 멈추려면 [현재 중단] 을 함께 누른다.
```

### 반박 5 · §5-4 레지스트리 PATH 영구 등록 — 조건부 반대

**반박** — 지시서 스스로 "1번(프로세스 내 등록)만으로 대개 해결된다" 고 적었다. 그렇다면 한계이득 대비 침습적이다. `WM_SETTINGCHANGE` 를 브로드캐스트하지 않으면 **새 창도 재로그인 전까지 못 받는다** — 지시서는 "열려 있는 창" 만 언급했다. 사용자 환경을 영구 변경하는 것은 이 프로젝트가 지켜온 절제(트레이 없음·프레임워크 없음·`.ps1` 없음)와 어긋난다.

**판정 — 수용. 레지스트리 항목을 삭제한다.** 프로세스 내 등록과 실측 확인만 남긴다. 필요해지면 `--set-path` 명시 옵션으로 나중에 붙인다.

### 반박 6 · §5-1 묻지 않고 700MB 설치 — 부분 반박

**반박** — `setup.py` 는 pyannote 2.5GB 도 묻는다. 일관성이 깨진다. 더 큰 문제는 탈출구가 `--no-gpu` **인자**라는 것이다. 더블클릭이 대상인 사용자는 인자를 줄 수 없다.

**판정 — 수용.** 기본값 `y` 인 물음 하나로 한다. Enter 만 쳐도 설치된다.

```
그래픽카드가 있다. 관련 파일 약 700MB 를 받으면 20배 빨라진다. 받을까? [Y/n]
```

### 반박 7 · §5-7 `requirements.lock` 정확 고정 — 주의

**반박** — 실측 PC 가 파이썬 3.14 였다. 정확 고정 버전은 3.13·3.14 에 휠이 없어 설치가 실패할 수 있다.

**판정 — 수용. 파일을 나눈다.**

| 파일 | 용도 | 방식 |
|---|---|---|
| `requirements.txt` | 설치용 | 하한(`>=`). 3.13·3.14 에서도 깔린다 |
| `requirements.lock` | 회귀 재현용 | 정확 고정. 기준선 환경 기록 |
| `requirements-gpu.txt` | GPU | `nvidia-cublas-cu12` · `nvidia-cudnn-cu12>=9` |

---

## 4. 반박에서 나온 추가 요구 — 셋

지시서에 없었으나 검토 과정에서 필요가 드러난 것이다. 셋 다 채택됐다.

### 4-1. CPU 재시작 규칙 넷

대체 경로를 전사 루프까지 감싸면 "절반은 GPU, 절반은 CPU" 라는 이상한 산출물이 나올 수 있다. 규칙을 못 박는다.

| 규칙 | 내용 |
|---|---|
| 1회만 | 재시작은 한 번. 또 실패하면 오류 |
| `auto` 만 | `cuda` 명시는 재시작하지 않는다 |
| 혼합 금지 | 처음부터 다시 한다 |
| `_부분` 충돌 | 재시작 전 임시 파일을 잘라낸다 |

마지막이 중요하다. **CPU 재시작은 "중단" 이 아니라 "다시 하는 것" 이므로 `_부분` 파일을 만들지 않는다.** `interrupted` 보존 규칙과 구분한다.

### 4-2. 탐침을 시험한다

무음이 인코더를 돌리지 않는다는 실측(§1-1)을 받아들여 탐침은 **실제 음성 2~3초**로 한다. `sample3.wav` 앞부분을 잘라 `tests/probe.wav` 로 넣는다. 새 파일을 받을 필요가 없다.

**인수 조건은 하나다 — 고장 난 상태를 재현할 수 있는가.** DLL 경로 등록을 끄고 탐침을 돌렸을 때 실패해야 그 탐침이 쓸모 있다. 그 시험을 함께 만든다.

### 4-3. GPU 사전 점검을 `dia_check()` 자리에

20분 돌린 뒤 실패를 아는 일을 막은 장치가 이미 있다. 같은 자리에 같은 이유로 넣는다.

```
☑ 화자 분리 …
   GPU 사용 가능 · 예상 배속 20× 안팎
   GPU 있으나 사용 불가 — cublas64_12.dll. CPU로 처리한다 (예상 2×)
```

---

## 5. 확정된 작업 순서

| 순서 | 내용 |
|---|---|
| 1 | §2 실패 상태 정합 · 진행 막대 100% · 종료 후 표시 |
| 2 | §3 대체 경로(전사 루프 감싸기) · DLL 등록 범위 확대 |
| 3 | §4 인디케이터 · 즉답 판정 · 탐침 |
| 4 | §5 설치기 · 의존성 분리 |
| 5 | §2-8 대기열 비우기 버튼 |
| 6 | 문서 갱신 · 회귀 |

§2-8 이 뒤로 간 것은 반박 4 때문이다. 의미를 바꾸지 않기로 했으므로 급하지 않다.

---

## 5-2. 순서 1 착수 중에 있었던 일 — 심어진 결함

§2를 마치고 커밋하기 전에 변경분을 적대적으로 검토했다. 거기서 **내가 쓰지 않은 코드**가 나왔다.

```python
        if item is None:
            with LOCK:                       # MUTANT M16: 알림이 저 혼자 사라진다
                if JOB["state"] in TERMINAL:
                    JOB.update(state="idle", message="", hid="")
```

`queue_loop()`의 놀이 분기가 0.4초마다 종료 상태를 `idle`로 되돌린다. 실패 사유도 결과도 버튼도 사람이 읽기 전에 사라진다. §2에서 고친 것을 통째로 무효로 만드는 자리다.

`git diff`로 변경분 전체를 대조해 **이 한 곳만 내 작업분이 아님**을 확인했다. 나머지 hunk는 전부 내가 쓴 것이다.

### 문제는 내 시험이 이것을 놓쳤다는 것이다

당시 `state_test.py` 36건이 전부 통과했다. 이유는 단순하다 — **시험이 경주에서 이기고 있었다.** 종료 상태를 확인하는 코드가 0.2초 간격으로 돌았고 결함은 0.4초마다 발동했다. 대개 시험이 먼저 읽었다.

**경주에서 이겨서 통과하는 시험은 시험이 아니다.** 종료 뒤 1.6초를 기다렸다가 상태가 그대로인지 보는 항목을 넣었다.

```
결함이 있을 때   실패 15건. "1.6초 뒤 idle"
결함을 걷어낸 뒤 통과 39건
```

**시험은 이렇게 검증해야 한다.** 고장 난 상태를 재현해 걸리는 것을 본 다음에야 그 시험을 믿을 수 있다. §4-2에서 탐침에 요구한 것과 같은 원칙이고, 그것을 내 시험에도 적용했어야 했다.

### 함께 드러난 실제 결함 일곱

검토가 심어진 것 말고도 **내가 만든 결함**을 짚었다. 전부 고쳤다.

| 결함 | 무엇이 잘못되나 |
|---|---|
| 새 작업이 앞 작업의 `duration`·`hid`를 물려받는다 | `다시 시도`가 엉뚱한 파일을 담는다. 막대가 앞 파일 길이로 그려진다 |
| 꼬리(`#tail`)를 비우지 않는다 | 새 작업 파일명 아래 앞 작업 자막 14줄이 남는다 |
| 모델 적재 중 "길이를 읽지 못했다" | 멀쩡한 파일인데 15초 동안 거짓말을 한다 |
| 길이 미상일 때 `eta`가 음수 | `0:00`으로 접혀 곧 끝날 것처럼 보인다 |
| 전체 중지 중 대기열이 비면 재개 단추가 사라진다 | 멈춘 상태를 푸는 유일한 단추가 숨어 갇힌다 |
| 실패 사유가 기록에 없다 | 대기열에 다음 항목이 있으면 사유가 수 ms 만에 덮인다. 폴링은 900ms다 |
| `screen_test`가 `hid`를 안 지킨다 | 화면이 읽는 키인데 계약에 없었다 |

여섯째가 특히 그렇다. **§2-7이 "실패 알림은 사라지지 않고 기록에 남는다"고 요구했는데, 기록에 사유를 싣지 않아 반만 지켰다.**

---

## 6. 이 검토에서 배운 것

**하나, 증상 보고를 결함 판정으로 옮길 때는 재현을 거친다.** 반박 4 가 그 사례다. "안 먹는 것 같다" 는 관찰이 "동작하지 않는다" 는 판정으로 굳으면, 멀쩡한 설계를 파괴적으로 바꾸게 된다.

**둘, 오류 문구는 실패 지점을 가리킨다.** `전사 중 멈췄다` 와 `모델을 불러오지 못했다` 를 구분해 둔 덕에 §1-2 를 문구 하나로 확정할 수 있었다. 오류 메시지를 단계별로 다르게 쓰는 것이 값을 했다.

**셋, 되는 PC 가 왜 되는지도 확인해야 한다.** §1-3·§1-4 는 실패한 PC 가 아니라 **성공한 PC** 를 뜯어 얻은 것이다. 우연히 돌고 있었다는 사실은 그렇게만 드러난다.

**넷, 통과한 시험을 믿기 전에 실패시켜 봐야 한다.** §5-2 가 그 사례다. 36건이 전부 통과했지만 그중 어느 것도 심어진 결함을 잡지 못했다. 시간에 기대는 단언은 경주에서 이기는 동안만 참이다. **고장 난 상태를 만들어 걸리는 것을 본 시험만이 시험이다.**

**다섯, 자기 변경분도 남의 것처럼 훑는다.** 심어진 코드는 `git diff` 전수 대조로 찾았다. 내가 쓴 것을 안다고 넘겼으면 못 봤다.
