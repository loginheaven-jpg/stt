# 받아쓰기 설치 안내

명령창을 몰라도 된다. 순서대로 따라오면 된다.

---

## 1. 이 프로그램이 하는 일

녹음 파일을 글로 옮겨 적는다.

말한 사람이 둘 이상이면 `화자1`, `화자2` 로 나눠 적는다. 소리가 이 PC 밖으로 나가지 않고 돈도 들지 않는다.

---

## 2. 준비물 — 파이썬

파이썬이라는 프로그램이 하나 필요하다. 무료다. 이미 깔려 있으면 3절로 건너뛴다.

**하나.** https://www.python.org/downloads/ 에 들어가 노란 단추를 누른다.

<!-- 스크린샷: 파이썬 다운로드 페이지. 노란 Download 단추가 보이게 -->

**둘.** 내려받은 파일을 실행하면 첫 화면이 뜬다. **맨 아래 `Add Python to PATH` 에 반드시 체크한다.**

<!-- 스크린샷: 파이썬 설치 첫 화면. Add Python to PATH 체크박스를 표시해서 -->

이 체크를 놓치면 설치를 해도 프로그램이 파이썬을 찾지 못한다. 가장 많이 막히는 곳이다.

체크한 뒤 `Install Now` 를 누른다. 끝나면 `Close` 를 누른다.

---

## 3. 설치

> 이미 쓰고 있는 PC에서 폴더를 복사해 온 것이라면 **8절을 먼저 본다.** 지우고 와야 할 것이 몇 가지 있다.

받아쓰기 폴더 안의 **`setup.py` 를 더블클릭한다.**

검은 창이 뜨고 열 단계가 차례로 지나간다. 화면이 묻는 것에만 답하면 된다.

| 묻는 것 | 어떻게 답하나 |
|---|---|
| 화자 분리를 쓸까? | `y`. 기본 기능이다. 혼자 말하는 강의 녹음만 다룰 거면 `n` |
| 2.5GB 를 내려받는다. 계속할까? | `y`. 시간이 걸린다 |
| 토큰 | 아래 참고. 건너뛰려면 그냥 Enter |
| **그래픽카드 파일 700MB 를 받을까?** | **`y`.** 그래픽카드가 있을 때만 묻는다. 20배쯤 빨라진다 |
| 모델을 지금 받을까? | `y`. 받아쓰기 1.6GB 와 화자 분리 31MB 를 함께 받는다 |

그냥 Enter 만 쳐도 무난한 쪽으로 간다.

마지막에 결과 요약이 뜬다. 항목마다 `통과`·`건너뜀`·`못 했다` 중 하나가 붙는다.

- **`건너뜀`** 은 내가 안 하겠다고 한 것이다. 문제가 아니다
- **`못 했다`** 만 신경 쓰면 된다. 그것도 대개는 그대로 쓸 수 있다

그래픽카드를 쓰기로 했다면 요약 앞에 **그래픽카드 점검** 표가 나온다. 여덟 줄이 모두 `통과` 여야 그래픽카드로 돈다. 하나라도 `못 함` 이면 그 줄이 원인이다.

```
  통과  드라이버              NVIDIA GeForce RTX 3060 · CUDA 12.6
  통과  ctranslate2         4.8.1 → CUDA 12 · cuDNN 9 계열이 필요하다
  통과  cublas64_12.dll     site-packages 안에서 찾았다
  통과  DLL 경로 등록         4곳
  통과  시험 전사              4.5초 · cuda
```

**마지막 줄이 핵심이다.** 2.8초짜리 음원을 실제로 받아써 본 결과다. 앞의 일곱 줄이
다 통과해도 이것이 `못 함` 이면 그래픽카드로 돌지 않는다.

요약을 읽고 나면 마지막에 `창을 닫으려면 Enter 를 누른다` 가 나온다. 그때 Enter 를 치면 창이 닫힌다.

### 화자 분리 토큰 받기

화자 분리를 쓸 때만 필요하다. 무료이고 한 번만 받으면 된다. 설치 화면이 이 순서를 다시 알려준다.

1. https://huggingface.co 에 가입한다
2. https://huggingface.co/pyannote/speaker-diarization-community-1 에 들어가 약관에 동의한다
3. https://huggingface.co/settings/tokens 에서 `Create new token` 을 누르고 종류를 **Read** 로 고른다
4. 만들어진 `hf_` 로 시작하는 값을 복사해 설치 화면에 붙여넣는다

검은 창에 붙여넣을 때는 **마우스 오른쪽 단추**를 누른다. Ctrl+V 는 안 될 수 있다.

2번을 건너뛰면 토큰이 있어도 화자 분리가 안 된다. 자주 놓치는 곳이다.

**한 번만 하면 된다.** 약관 동의는 PC 가 아니라 HuggingFace 계정에 붙는다. 같은 계정을 쓰는 한 새 PC 에서 다시 동의할 필요가 없다. 토큰만 넣으면 된다.

---

## 4. 첫 실행

**바탕화면의 `받아쓰기` 를 더블클릭한다.** 검은 창은 뜨지 않고 브라우저만 열린다.

바로가기가 없으면 받아쓰기 폴더의 `start.vbs` 를 더블클릭한다.

브라우저가 안 열리면 주소창에 직접 넣는다.

```
http://127.0.0.1:8765
```

녹음 파일은 받아쓰기 폴더 안의 **`audio` 폴더**에 넣는다. 넣은 뒤 화면에서 `새로 고침` 을 누르면 목록에 나온다.

---

## 5. 화면 사용법

한 화면에 네 칸이 있다. 위에서부터 **지금 하는 일 · 대기열 · 담기 · 최근 기록** 순이다.

**처음 열면 두 칸만 보인다.** 하는 일이 없으면 위의 두 칸은 숨는다. 파일을 담으면 그때 나타난다. 없어진 것이 아니다.

### 머리말 — 그래픽카드 상태

맨 위 제목 옆에 늘 배지 하나가 떠 있다. 셋 중 하나다.

| 배지 | 뜻 |
|---|---|
| `GPU 사용 가능` | 그래픽카드로 돈다. 빠르다 |
| `CPU만 사용` | 그래픽카드가 없다. 느리지만 결과는 같다 |
| **`GPU 있으나 사용 불가`** | **그래픽카드는 있는데 못 쓰고 있다.** 7절을 본다 |

**누르면 진단 화면으로 간다.**

세 번째는 처음부터 뜨지 않는다. 실제로 한 번 돌려 보거나 진단에서 탐침을 눌러 실패한 뒤에야 나온다.

### 담기

아래에서 두 번째 칸이다. 여기서 일을 만든다.

1. 목록에서 녹음 파일을 고른다. **여러 개를 골라도 된다**
2. `설정` 에서 `코칭 시연` 이나 `강의 받아쓰기` 를 고른다
3. `출력 폴더` 는 그대로 둬도 된다
4. `대기열에 담기` 를 누른다

**처음 상태 그대로 담아도 된다.** 화자 분리가 켜져 있고, 사람 수는 `자동 추정` 이고, 결과는 `시각 포함 txt` 와 `정본화 초안 md` 두 가지로 나온다. 코칭 대화를 받아쓰는 데 맞춘 기본값이다.

몇 명인지 알면 `자세히` 에서 골라 주는 편이 정확하다. 1명부터 10명까지 있다. **모르면 `자동 추정` 으로 둔다** — 틀린 수를 지정하면 자동 추정보다 나빠진다.

**이름과 전문용어를 적어두면 훨씬 정확해진다.** `자세히` 를 펼치면 `이름·전문용어` 칸이 있다. 녹음에 나오는 사람 이름을 쉼표로 나눠 적는다.

```
이승은, 황미혜, 스쿼트, 에스컬레이터
```

회색 예시 글씨는 그냥 예시다. 직접 적어야 반영된다.

### 지금 하는 일

맨 위 칸이다. 담자마자 돌기 시작한다.

큰 숫자가 **배속**이다. `7.2×` 면 1시간짜리를 8분 만에 끝낸다는 뜻이다. 아래 막대는 녹음 시간 기준이고, 받아쓴 내용이 실시간으로 흘러간다.

배속 옆의 **`[GPU]`** 또는 **`[CPU]`** 가 지금 무엇으로 돌고 있는지 알려준다. 그 숫자가 왜 그런지를 설명하는 표시다.

**`[CPU · GPU 사용 불가]`** 로 나오면 그래픽카드가 있는데 못 쓰고 있는 것이다. 바로 아래에 사유가 한 줄 붙는다. 7절을 본다.

`현재 중단` 은 지금 것만 멈추고 다음으로 넘어간다. `전체 중지` 는 **지금 것을 끝낸 뒤** 멈춘다. 멈춰 있는 동안 단추 이름이 `대기열 재개` 로 바뀌고, 다시 누르면 이어서 돈다.

`전체 중지` 를 누르면 무슨 일이 일어나는지 아래에 적힌다.

```
지금 것을 끝내면 멈춘다. 남은 3건은 그대로 있다.
즉시 멈추려면 [현재 중단] 을 함께 누른다.
```

**어느 쪽도 이미 받아쓴 내용을 지우지 않는다.**

### 끝났을 때와 실패했을 때

작업이 끝나면 단추가 바뀐다.

| 상태 | 보이는 단추 |
|---|---|
| 도는 중 | `현재 중단` · `전체 중지` |
| 끝남 | `닫기`. 아래에 저장 위치가 나온다 |
| 실패·중단됨 | `다시 시도` · `닫기` |

`다시 시도` 는 같은 설정으로 대기열에 다시 넣는다. `닫기` 는 알림만 치운다 — 기록은 남는다.

끝나면 `남은 시간` 이 사라지고 `경과` 가 **`총 소요`** 로 바뀐다. 막대는 100% 까지 찬다.

실패하면 왜 그런지와 무엇을 하면 되는지가 함께 나온다.

```
강의녹음.m4a — 전사 중 멈췄다. Library cublas64_12.dll ...
→ GPU를 쓸 수 없다. 설정에서 기기를 cpu 로 바꾸거나 진단 화면을 확인해달라.
```

**막대는 실패한 지점에 그대로 멈춰 있다.** 어디까지 갔는지 보여야 하기 때문이다. 그때까지 받아쓴 내용은 저장돼 있다.

### 그래픽카드가 말썽이면 스스로 CPU 로 바꾼다

기기를 `auto`(기본값)로 두었을 때만이다. 받아쓰는 도중 그래픽카드 쪽에서 문제가 나면 **처음부터 CPU 로 다시 시작한다.** 화면에 이렇게 나온다.

```
GPU를 쓸 수 없어 CPU로 다시 시작했다. Library cublas64_12.dll ...
```

반쯤 받아쓴 것은 버리고 새로 한다. 절반은 그래픽카드로, 절반은 CPU 로 받아쓴 뒤죽박죽 결과가 나오지 않게 하기 위해서다. **느려질 뿐 결과는 온전하다.**

다시 시작은 한 번뿐이다. 그래도 안 되면 오류로 끝난다. `자세히` 에서 기기를 `cuda` 로 못 박아 두었다면 바꾸지 않고 그대로 실패한다 — 조용히 느려지면 원인을 못 찾기 때문이다.

**그래서 기기는 `auto` 로 두기를 권한다.** 옆에 지금 판정이 붙어 있다.

| 보이는 것 | 뜻 |
|---|---|
| `auto · GPU 사용` | 그래픽카드로 돈다. `cuda` 로 바꿔도 더 빨라지지 않는다 |
| `auto · CPU만` | 그래픽카드가 없다 |

`auto` 는 그래픽카드가 있으면 알아서 쓴다. **`cuda` 로 바꾸면 빨라지는 것이 아니라 안전장치만 없어진다.**

### 대기열

남은 일이 순서대로 보인다. `▲▼` 로 순서를 바꾸고 `✕` 로 뺀다. 지금 돌고 있는 것은 옮기거나 뺄 수 없다.

오른쪽 위 **`대기열 비우기`** 를 누르면 대기 중인 것을 한꺼번에 뺀다. 한 번 더 묻는다. **돌고 있는 것은 그대로 두고, 이미 만들어진 파일도 지우지 않는다.**

**여러 개를 걸어두고 자리를 떠도 된다.** 하나가 실패해도 다음이 계속된다. 프로그램을 껐다 켜도 남은 일이 살아 있다.

껐다 켰을 때 돌던 중이던 것은 **`중단됨`** 으로 붉게 표시되고 단추 셋이 붙는다.

- `보존하고 다시` — 이미 만들어진 결과를 `이름_부분.txt` 로 옮겨두고 처음부터 다시 한다
- `덮어쓰고 다시` — 그냥 처음부터 다시 한다
- `빼기` — 하지 않는다

**저절로 다시 돌지 않는다.** 이미 받아쓴 것을 덮어쓸 수 있어서 사람에게 묻는다.

### 최근 기록

끝난 일이 쌓인다.

- `열기` — 결과 파일을 본다
- `같은 설정으로` — 같은 조건으로 다시 돌린다. **이름을 보태서 다시 돌릴 때 쓴다**
- `삭제` — 기록에서 지운다

결과 파일은 `out_text` 폴더에 쌓인다.

| 파일 | 내용 |
|---|---|
| `이름.txt` | 글만 |
| `이름_timed.txt` | 시각이 붙은 글 |
| `이름_canon.md` | 화자와 시각이 붙은 정리본 |
| `이름.srt` | 자막 |
| `이름_corrections.md` | 이름·용어를 고친 내역 |

### 종료

오른쪽 위 **`종료`** 를 누른다. 작업 관리자를 열 일이 없다. 하던 일이 있으면 한 번 더 묻는다.

---

## 6. 문제가 생기면

`최근 기록` 칸 오른쪽의 **`진단`** 을 누른다. 머리말의 그래픽카드 배지를 눌러도 같은 곳으로 간다.

파이썬 버전, **그래픽카드 상태**, 깔린 패키지, 토큰 유무, 내려받은 모델, 폴더 위치가 한자리에 나온다. 맨 아래에 최근 기록 200줄이 있다.

그래픽카드 절에는 **`탐침 실행`** 단추가 있다. 2.8초짜리 음원을 실제로 받아써 보고 되는지 확인한다. 몇 초 걸린다.

빨간 글씨가 있으면 그것이 원인이다. 도움을 청할 때 이 화면을 통째로 보여주면 빠르다.

---

## 7. 자주 막히는 곳

전부 실제로 겪은 것이다.

### `python은(는) 내부 또는 외부 명령이 아닙니다`

파이썬을 설치할 때 `Add Python to PATH` 에 체크하지 않았다.

파이썬을 다시 설치한다. 첫 화면에서 체크박스를 확인한다. 이미 깔려 있으면 설치 화면에 `Modify` 가 나오는데, 지우고 다시 까는 편이 빠르다.

### 화면이 안 열린다

브라우저가 저절로 열리지 않았을 뿐 프로그램은 돌고 있다. 주소창에 직접 넣는다.

```
http://127.0.0.1:8765
```

### 고쳤는데 화면이 그대로다

이전 프로그램이 아직 살아 있다. 열려 있는 받아쓰기 화면에서 **`종료`** 를 누르고 다시 실행한다.

그래도 그대로면 브라우저가 옛 화면을 붙들고 있는 것이다. **Ctrl+F5** 를 누른다.

### 화자 분리가 안 된다

셋 중 하나다.

- 토큰을 넣지 않았다
- HuggingFace 에서 **약관에 동의하지 않았다** (3절 토큰 받기의 2번)
- 토큰 종류를 `Read` 가 아닌 것으로 만들었다

설치할 때 화자 분리 모델을 받으므로 대개 그 자리에서 걸린다. `못 했다  화자 분리 모델` 이 뜨고 무엇을 해야 하는지 화면에 나온다.

이미 설치를 마친 뒤라면 `진단` 을 눌러 확인한다. 고친 뒤에는 `setup.py` 를 다시 더블클릭하면 된다. 다른 설정은 건드리지 않는다.

### 이름이 계속 틀리게 적힌다

`자세히` 의 `이름·전문용어` 칸이 비어 있다.

회색 글씨는 예시일 뿐 아무 일도 하지 않는다. 녹음에 나오는 이름을 직접 적는다. 이미 돌린 것은 `최근 기록` 에서 `같은 설정으로` 를 누르고 이름을 보태 다시 돌리면 된다.

고친 내역은 `이름_corrections.md` 에 남는다. **`보류` 라고 적힌 것은 본문을 건드리지 않은 것이다.** 확실하지 않으면 고치지 않는다.

### 너무 느리다

그래픽카드가 없으면 원래 느리다. 실시간의 2배 안팎이 한계다. 3시간짜리면 1시간 30분쯤 걸린다.

`자세히` 에서 모델을 건드린 적이 있다면 **`large-v3-turbo`** 로 되돌린다. 이것이 기본값이고 가장 빠르다. `large-v3` 는 훨씬 느린데 결과 차이는 크지 않다.

그래도 느리면 여러 개를 걸어두고 자리를 뜨는 편이 낫다. 대기열은 그러라고 만든 것이다.

### 그래픽카드가 있는데 CPU 로 돈다

머리말에 **`GPU 있으나 사용 불가`** 가 떠 있으면 이것이다. 배속도 20 배쯤에서 2 배로 떨어진다.

**`진단` 을 눌러 `그래픽카드` 절을 본다.** 여덟 줄 중 어디에서 `못 함` 이 나는지가 원인이다. `탐침 실행` 을 누르면 2.8 초짜리 음원으로 실제로 확인한다.

| 어디서 막혔나 | 무엇을 하나 |
|---|---|
| 드라이버 | 그래픽카드 드라이버가 오래됐다. **`CUDA` 가 12.0 이상**이어야 한다 |
| `nvidia-cublas` · `nvidia-cudnn` | 관련 파일을 안 받았다. `setup.py` 를 다시 더블클릭한다 |
| `cublas64_12.dll` | 파일은 있는데 프로그램이 못 찾는다. 프로그램을 껐다 켠다 |
| 시험 전사 | 앞이 다 통과했는데 여기서 막히면 드라이버를 갱신해 본다 |

대개는 **`setup.py` 를 다시 더블클릭하면 해결된다.** 그래픽카드용 파일을 받고 경로를 잡아준다.

못 고쳐도 받아쓰기는 된다. 느릴 뿐이다.

### 결과 파일이 안 보인다

`최근 기록` 에서 `열기` 를 누르면 바로 뜬다.

폴더에서 찾으려면 받아쓰기 폴더 안의 `out_text` 를 연다. 담을 때 출력 폴더를 바꿨으면 그쪽이다.

### 프로그램이 갑자기 사라졌다

받아쓰기 폴더의 `data` 안에 `app.log` 가 있다. 메모장으로 열면 무슨 일이 있었는지 마지막 줄에 남아 있다.

받아쓰던 내용은 구간마다 저장되므로 **거기까지는 남아 있다.** 다시 켜면 대기열에 `중단됨` 으로 나온다. 5절 대기열 항목의 단추 셋 중에서 고르면 된다.

---

## 8. 다른 PC에 옮기기

받아쓰기 폴더를 통째로 복사하면 된다. 설치 파일 같은 것은 따로 없다.

```
1. 새 PC에 파이썬을 깐다          ← 2절. Add Python to PATH 체크
2. 받아쓰기 폴더를 복사해 넣는다   ← 아래 것들을 지우고
3. setup.py 를 더블클릭한다        ← 3절
4. 바탕화면의 [받아쓰기] 를 누른다  ← 4절
```

### 인터넷에서 받는 편이 더 깨끗하다

USB 로 옮기는 대신 인터넷에서 내려받아도 된다. **이쪽이 더 낫다.** 지울 것을 신경 쓸 필요가 없다.

1. https://github.com/loginheaven-jpg/stt 에 들어간다
2. 초록색 **`Code`** 단추 → **`Download ZIP`**
3. 내려받은 압축 파일을 풀고, 그 폴더에서 `setup.py` 를 더블클릭한다

이렇게 받으면 프로그램과 문서만 들어 있다. 1MB 도 되지 않고, 아래에서 말하는 "지울 것" 이 처음부터 없다.

USB 로 옮겨야 하는 상황이면 아래를 따른다.

### 복사하기 전에 지울 것

폴더 전체가 6MB 남짓이라 크기는 문제가 아니다. **지우는 이유는 옛 PC 의 흔적 때문이다.**

| 지울 것 | 왜 |
|---|---|
| **`data` 폴더** | 옛 PC 기준의 설정과 기록이 들어 있다. 출력 폴더 목록이 옛 경로를 가리키고, 최근 기록이 새 PC 에 없는 파일을 가리킨다 |
| **`out_text` 폴더** | 지난 결과물이다. 녹음에 나온 사람 이름이 그대로 들어 있으니 내 PC 가 아니면 특히 지우고 옮긴다 |
| **`__pycache__` 폴더** | 옛 PC 에서 만들어진 임시 파일이다 |

셋 다 지워도 새 PC 에서 다시 만들어진다.

`audio` 폴더 안의 `sample3.wav` 와 폴더 맨 위의 `sample3.wav` 는 만든 사람이 시험용으로 넣어둔 26 초짜리 음원이다. 같은 파일이 두 벌 있는 것이고 지워도 프로그램은 똑같이 돈다. 이것까지 지우면 1MB 도 되지 않는다.

`env.local` 은 골라서 한다.

- **그대로 가져가면** — 화자 분리 토큰을 다시 넣지 않아도 된다
- **지우고 가면** — `setup.py` 가 토큰을 다시 묻는다

내 PC 가 아니라면 지우고 가는 편이 낫다. 이 파일에는 토큰 같은 개인 열쇠가 글자 그대로 들어 있다.

어느 쪽이든 **HuggingFace 약관에 다시 동의할 필요는 없다.** 동의는 PC 가 아니라 계정에 붙는다. 같은 계정의 토큰이면 새 PC 에서 그대로 통한다.

### 모델은 폴더에 없다

가장 놓치기 쉬운 부분이다. 받아쓰기 모델은 폴더 안이 아니라 **사용자 계정 폴더**에 들어 있다.

```
C:\Users\<내 계정>\.cache\huggingface\hub
```

그래서 폴더를 복사해도 **새 PC 는 모델을 다시 받는다.** 새 PC 에 여유 공간 2GB 와 내려받을 시간이 필요하다.

| 모델 | 크기 | 언제 받나 |
|---|---|---|
| 받아쓰기 모델 | 약 1.6GB | `setup.py` 6단계 |
| 화자 분리 모델 | 약 31MB | `setup.py` 6단계 |

**둘 다 설치할 때 받는다.** 6단계에서 `y` 를 누르면 그 자리에서 끝난다. 여기서 건너뛰면 첫 작업이 시작될 때 받느라 한참 멈춘 것처럼 보이므로, 시간이 있으면 설치할 때 받아두는 편이 낫다.

### 폴더 위치는 아무 데나

`D:\받아쓰기` 든 바탕화면이든 상관없다. 프로그램이 자기 위치를 스스로 찾는다.

**다만 나중에 폴더를 옮기면 바탕화면 바로가기가 끊긴다.** 그때는 `setup.py` 를 다시 더블클릭하면 바로가기가 새로 만들어진다.

### 새 PC 가 느릴 수 있다

그래픽카드(GPU)가 있느냐에 따라 속도가 크게 갈린다. 1시간짜리 녹음 기준이다.

| 새 PC | 받아쓰기만 | 화자 분리까지 |
|---|---|---|
| 그래픽카드 있음 · **설치 때 GPU 파일을 받았을 때** | 3분 안팎 | 6분 안팎 |
| 그래픽카드 있음 · GPU 파일을 안 받았을 때 | 30분 안팎 | 그보다 더 |
| 그래픽카드 없음 | 30분 안팎 | 그보다 더 |

**가운데 줄을 조심한다.** 그래픽카드가 있어도 설치 때 3단계의 GPU 물음에 `n` 을 눌렀으면 CPU 로 돈다. 머리말 배지가 `CPU만 사용` 이나 `GPU 있으나 사용 불가` 로 나오면 그 경우다. `setup.py` 를 다시 돌리면 된다.

그래픽카드 있는 쪽은 만든 PC(RTX 3060)에서 실제로 잰 값이고, 없는 쪽은 어림값이다.

느려도 쓰는 방법은 같다. 대기열에 여러 개를 걸어두고 자리를 뜨면 된다. 오히려 느린 PC 일수록 대기열이 쓸모 있다.

너무 느리면 7절의 마지막 항목을 본다.
