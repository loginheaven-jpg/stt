# ── 비밀값 ────────────────────────────────────────────────
# API 키와 토큰이 들어간다. 절대 올리지 않는다.
env.local
.env.local
.env
env.txt

# ── 음원 ──────────────────────────────────────────────────
# 소스가 아니라 데이터다. GitHub 파일당 한도는 100MB다.
*.wav
*.mp3
*.m4a
*.mp4
*.flac
*.ogg
*.aac
*.wma
*.webm
*.mkv
*.mov
*.amr
*.opus

# 예외 하나. GPU 탐침용 2.8초 음원이다. 89KB 이고 이름이 나오지 않는 구간이다.
# 이것이 없으면 "고장 난 상태를 재현하는가" 를 시험할 수 없다.
!tests/probe.wav

# ── 제3자 전사문 ──────────────────────────────────────────
# 회귀 기준선(baseline/)과 무관한 다른 세션의 녹취다. 실명이 여럿 나온다.
# convert_clovanote.py 의 시험 자료이지만 당사자 동의 범위 밖이라 올리지 않는다.
sample_.txt

# ── 실행 산출물 ───────────────────────────────────────────
# app.py 와 run_stt.py 가 만든다. 돌리면 다시 생긴다.
out_text/
out/
out2/
out_sample3/
out_*/

# 대기열·이력·설정과 기록 파일
data/

# 회귀 기준선(baseline/)은 산출물이지만 동결한 자산이므로 올린다.

# ── 파이썬 ────────────────────────────────────────────────
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/

# ── 편집기·OS ─────────────────────────────────────────────
.vscode/
.idea/
Thumbs.db
desktop.ini
*.bak
*.tmp
