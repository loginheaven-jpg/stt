# Phase 1 지시서 보완 — 회귀 기준 재정의와 추가 요구

> 원 지시서 : `stt_app_spec_phase1.md`
> 이 문서가 우선한다. 충돌하는 조항은 이 문서를 따른다.
> 작성일 : 2026-08-13

---

## 1. 지적 판정

`구축계획.md`의 여섯 지적을 검토했다. 판정은 이렇다.

| # | 지적 | 판정 | 조치 |
|---|---|---|---|
| 1 | §9-1 회귀 게이트 불성립 | **타당** | §2에서 재정의 |
| 2 | §1-3 수치의 산출물 부재 | **타당** | §3에서 지위 변경 |
| 3 | 채점기 cp949 오류 | 타당 | §4-1 |
| 4 | 무창 실행 시 출력 소실 | **타당. 가장 중요** | §4-2 |
| 5 | `.vbs` 한글 경로 | 타당 | §4-3 |
| 6 | `/open` 경로 검사 취약 | 타당 | §4-4 |

여섯 모두 받아들인다. 특히 4번은 지시서가 놓친 것이다.

**0단계(기준선 복원·동결) 신설을 승인한다.** 기준선 없이 1단계 인수를 판정할 방법이 없다는 지적이 옳다.

---

## 2. 내 오류 둘 — 먼저 밝힌다

### 2-1. §9-1의 명령이 잘못됐다

지시서는 이 명령을 적었다.

```
python stt_bench.py sample3.txt "out_text/이승은코치_canon.md" ...
```

**실제 측정은 이렇게 하지 않았다.** `이승은코치_canon.md`에서 00:19 이하 구간만 잘라낸 임시 파일을 만들어 재었다. 그 절단 단계를 지시서에 옮기지 않았다. 30어절 정답지에 18분 전사문 전체를 맞대게 한 것은 내 누락이다.

### 2-2. §1-3의 첫 행은 측정이 아니라 시뮬레이션이다

`화자정답률 73.3% · 고유명사 100%`는 **이름·용어 칸을 비운 채 만든 산출물에 `fix_terms()`를 사후 적용해 얻은 값**이다. 칸을 채웠을 때의 기댓값으로는 타당하나, 앱을 그 설정으로 돌려 얻은 결과가 아니다.

지시서에서 이를 측정치처럼 적었다. **재현 불가능한 수치를 인수 조건으로 삼은 것이 잘못이다.**

---

## 3. §1-3의 지위 변경

§1-3 비교표는 **무료 전용 결정의 근거**이지 인수 조건이 아니다. 표 아래에 이 문장을 붙인 것으로 간주한다.

> 이 표는 별도 세션에서 `run_stt.py`와 `stt_bench.py`로 측정한 값이며 저장소에 산출물이 없다. 판단 근거로만 쓰고 회귀 기준으로 쓰지 않는다. 첫 행의 무료 조합 수치는 이름·용어 교정을 사후 적용한 추정치다.

**Phase 1에서 이 표를 재현할 의무는 없다.** 유료 엔진을 다시 돌릴 이유도 없다.

---

## 4. 회귀 기준 재정의 — §9-1을 대체한다

### 4-1. 설계 원칙

정답지에 시각을 붙이는 방식은 **채택하지 않는다.** 두 가지 이유다.

**하나, 편향이 생긴다.** 시각을 어느 엔진의 출력에서 가져오든 그 엔진에 유리한 정답지가 된다. Phase 2에서 유료 엔진을 다시 재려 할 때 기준이 오염돼 있다.

**둘, 필요가 없다.** `sample3.wav`가 26.7초이고 `sample3.txt`가 그 전체를 덮는다. **음원 전체를 정답지가 덮으면 구간 절단 자체가 불필요하다.**

따라서 회귀 시험의 음원을 `이승은코치.m4a`에서 **`sample3.wav`로 바꾼다.**

정답지 확인 사항 하나. `sample3.txt`가 26.7초 전 구간을 덮는지 귀로 확인하고, 빠진 발화가 있으면 채운다. 시각은 넣지 않는다.

### 4-2. R1 — 정확도 회귀 (엄격)

| 항목 | 값 |
|---|---|
| 음원 | `audio/sample3.wav` |
| 정답지 | `sample3.txt` (시각 없음, 화자 표기 있음) |
| 설정 | 모델 `large-v3-turbo` · 기기 `auto` · 정밀도 `int8` · 언어 `ko` · beam 5 · 무음 500 |
| | VAD 켬 · 재시도 켬 · 말버릇 살리기 끔 · **이름·용어 교정 켬** |
| | 이름·용어 `이승은, 황미혜, 이대표님` |
| | 화자 분리 켬 · 화자 수 2 · 민감도 `high` |
| 형식 | 정본화 초안 md |

```
set PYTHONIOENCODING=utf-8
python stt_bench.py sample3.txt "out_text/sample3_canon.md" --nouns 이승은 황미혜 이대표님 --detail
```

**기준값은 0단계에서 실측해 동결한다.** 지시서 §9-1의 다섯 수치는 폐기한다.

동결 규칙이다.

| 지표 | 허용 |
|---|---|
| 화자 오귀속률 | 기준값 + 2%p 이내 |
| 화자정답률 | 기준값 − 3%p 이내 |
| CER | 기준값 + 2%p 이내 |
| 고유명사 재현율 | **기준값과 동일하거나 상승** |
| 삽입 어절 | **기준값과 동일하거나 감소** |
| 화자 수 | **정확히 2** |

허용폭을 둔 이유는 pyannote와 Whisper에 미세한 비결정성이 있기 때문이다. 고유명사와 삽입은 결정적 로직이므로 폭을 주지 않는다.

**0단계에서 같은 설정으로 3회 반복해 편차를 확인하고, 편차가 위 허용폭을 넘으면 그 사실을 보고한다.** 넘으면 허용폭을 조정한다.

### 4-3. R2 — 구조 회귀 (긴 파일)

26.7초만으로는 긴 파일에서의 회귀를 못 잡는다. 정답지 없이 구조만 본다.

| 항목 | 값 |
|---|---|
| 음원 | `audio/이승은코치.m4a` (18:15) |
| 설정 | R1과 동일. 이름·용어에 `스쿼트, 에스컬레이터, 헬스클럽, 대중교통` 추가 |

| 지표 | 허용 |
|---|---|
| 화자 수 | 정확히 2 |
| 발화 수 | 기준값 ±5% |
| 어절 총량 | 기준값 ±2% |
| 교정 건수 | 기준값과 동일 |
| 종료 시각 | 기준값 ±3초 |
| 배속 | 기준값 −20% 이내 |

**어절 총량 ±2%가 핵심이다.** 텍스트가 소리 없이 사라지는 회귀를 잡는다.

배속 항목은 대기열 도입 시 모델 재로딩으로 성능이 떨어지는 것을 잡는다.

### 4-4. 기준선 산출물

0단계 종료 시 저장소에 이것들이 남아야 한다.

```
baseline/
  sample3_canon.md            R1 산출물
  sample3_corrections.md
  이승은코치_canon.md          R2 산출물
  이승은코치_corrections.md
  baseline.json               측정치와 설정과 명령
  README.md                   재현 절차
```

`baseline.json`에 다음을 담는다.

```jsonc
{
  "created": "2026-08-__",
  "app_version": "2026-08-13.7",
  "env": { "python": "3.12.3", "faster_whisper": "…", "pyannote_audio": "…",
           "torch": "…", "device": "cpu" },
  "settings": { },
  "R1": { "runs": 3, "cer": 0.0, "spk_error": 0.0, "spk_correct": 0.0,
          "filler": 0.0, "nouns": 0.0, "insert": 0, "speakers": 2 },
  "R2": { "speakers": 2, "segments": 0, "words": 0, "corrections": 0,
          "end_sec": 0, "speed": 0.0 }
}
```

---

## 5. 자산 동결 규칙의 완화

`stt_bench.py`는 **채점 로직 변경을 금지한다.** 다만 입출력 안전 수정은 허용한다.

| 허용 | 금지 |
|---|---|
| `sys.stdout.reconfigure(encoding="utf-8", errors="replace")` | 지표 계산식 변경 |
| 인코딩·개행 처리 | 임계값 변경 (`HALLUCINATION_LIMIT` 등) |
| 예외 처리 보강 | 간투사 목록 변경 |
| — | 출력 항목 추가·삭제 |

수정 시 그 사실과 이유를 `baseline/README.md`에 적는다.

**우선순위는 `PYTHONIOENCODING=utf-8` 환경변수다.** 그것으로 해결되면 파일을 건드리지 않는다.

---

## 6. 추가 요구 — 지시서에 없던 것

### 6-1. 무창 실행 로깅 (최우선)

`pythonw.exe`에서 `sys.stdout is None`이고 `print()`가 조용히 무시된다는 지적이 옳다. 현재 `app.py`에 18곳의 `print()`가 있고 **전부 사라진다.** 그중에는 화자 분리 실패 사유와 포트 점유 안내가 들어 있다.

| 요구 | 내용 |
|---|---|
| 로그 파일 | `data/app.log`. 회전 (5MB × 3) |
| 이중 출력 | 콘솔이 있으면 콘솔과 파일에, 없으면 파일에만 |
| 최소 기록 | 시작·종료 · 작업 시작·종료 · 실패 사유 · 포트 충돌 · 설정 파일 읽기 결과 |
| 화면 노출 | 진단 화면에서 최근 200줄을 볼 수 있다 |
| 미기록 | **토큰·키 값은 절대 남기지 않는다.** 길이와 앞 4자까지만 |

**3단계 이전에 1단계에서 넣는다.** 로그 없이 무창 실행을 만들면 이후 모든 실패가 흔적 없이 사라진다.

### 6-2. 모델 재사용 — 대기열의 숨은 함정

현재 `transcribe()`가 호출될 때마다 `WhisperModel`을 새로 만든다. 대기열에 5건을 걸면 **모델을 5번 로딩한다.** CPU에서 회당 30~60초다. pyannote 파이프라인도 같다.

| 요구 | 내용 |
|---|---|
| 캐시 | `(model, device, compute)` 조합을 키로 보관 |
| 재사용 | 대기열의 다음 항목이 같은 조합이면 그대로 쓴다 |
| 해제 | 조합이 바뀌면 이전 것을 버리고 새로 만든다 |
| 유휴 해제 | 대기열이 비고 10분이 지나면 메모리를 놓는다 |
| pyannote | 같은 방식으로 캐시 |

R2의 배속 지표가 이 요구의 검증 수단이다.

### 6-3. 중단된 작업의 재시작 처리

`running` → `waiting` 복원은 위험하다. 부분 산출 파일이 이미 있는데 처음부터 다시 돌면 **덮어쓴다.**

| 상황 | 처리 |
|---|---|
| 재시작 시 `running` 항목 발견 | 상태를 `interrupted`로 바꾼다. 자동 재실행하지 않는다 |
| 화면 표시 | "중단된 작업입니다. 다시 하시겠습니까?" |
| 기존 산출물 | `이름_부분.txt`로 보존하거나 덮어쓸지 사용자가 고른다 |

### 6-4. 잠금 순서

`STATE_LOCK`과 `LOCK`을 분리한다면 **획득 순서를 하나로 고정한다.** 문서에 명시하고 코드 주석으로 남긴다. 두 잠금을 서로 다른 순서로 잡는 경로가 하나라도 생기면 교착한다.

권장 순서 — `STATE_LOCK` → `LOCK`. 역순 획득 금지.

### 6-5. `/open` 경로 검사

`startswith`를 쓰지 않는다. 확인했다.

```
out_text_비밀/x.txt   → startswith 통과, commonpath 차단
```

드라이브가 다르면 `os.path.commonpath`가 예외를 던지므로 감싼다. 항목별 출력 폴더를 허용하므로 **검사 대상은 "설정에 등록된 출력 폴더 목록"이어야 한다.** 임의 경로를 그때그때 받지 않는다.

### 6-6. `.vbs` 규칙

| 요구 | 내용 |
|---|---|
| 인코딩 | ANSI 또는 UTF-16LE(BOM). **UTF-8 금지** |
| 내용 | **한글을 넣지 않는다.** 주석도 영문으로 |
| 경로 | 하드코딩 금지. 자기 위치에서 얻는다 |
| 역할 | 최소한으로. 파이썬을 띄우는 일만 한다 |

안내 문구가 필요하면 `.vbs`가 아니라 파이썬 쪽에서 낸다. **인코딩 위험이 있는 파일에 로직을 두지 않는다.**

---

## 7. 승인 사항

`구축계획.md`의 다음 판단을 승인한다.

| 항목 | 판정 |
|---|---|
| 0단계 신설 | 승인 |
| `POST /start`를 큐 1건 투입으로 속만 교체 | **승인. 좋은 설계다** |
| 실행기 스레드 하나로 동시 실행 구조적 차단 | 승인 |
| `OUTDIR` 전역 제거와 함수 서명 변경 | 승인 |
| 진행률로는 저장하지 않는 규칙 | 승인 |
| 바탕화면 경로를 레지스트리에서 조회 | 승인 |
| `PYTHONIOENCODING=utf-8` 고정 | 승인 |

`POST /start`를 유지한 채 속을 바꾸는 전략이 특히 옳다. **기존 화면이 그대로 도는 것이 1단계 회귀 판정의 전제다.**

---

## 8. 0단계 지시

| 순서 | 할 일 |
|---|---|
| 1 | `sample3.txt`가 `sample3.wav` 전 구간을 덮는지 확인. 빠진 발화를 채운다. **시각은 넣지 않는다** |
| 2 | 로깅(§6-1)을 먼저 넣는다. 이후 모든 단계의 진단 수단이다 |
| 3 | §4-2 설정 그대로 `sample3.wav`를 3회 전사 |
| 4 | §4-3 설정으로 `이승은코치.m4a`를 1회 전사 |
| 5 | `baseline/` 생성. 산출물과 `baseline.json`과 재현 절차 |
| 6 | 3회 편차를 보고. 허용폭 조정이 필요하면 제안 |

**5번까지 마치고 보고한다.** 기준값을 확인한 뒤 1단계로 넘어간다.

보고에 포함할 것이다.

- R1 3회 측정치와 편차
- R2 측정치
- `sample3.txt`를 고쳤다면 무엇을 어떻게
- 로깅으로 드러난 기존 결함이 있으면 그 내용
- 허용폭 조정 제안이 있으면 근거와 함께

---

## 9. 이 보완서가 바꾸는 조항

| 원 지시서 | 변경 |
|---|---|
| §1-3 표 | 근거 자료로 지위 변경. 인수 조건 아님 |
| §9-1 전체 | **폐기.** §4로 대체 |
| §2 "수정 금지" (stt_bench.py) | §5로 완화 |
| §10 작업 순서 | 0단계를 앞에 넣는다 |
| §3-1 대기열 | §6-2 모델 캐시, §6-3 중단 처리 추가 |
| §6-2 유지 항목 | §6-1 로깅 추가 |

나머지 조항은 그대로다. 특히 **§6-1 금지 조항 아홉 개는 변경 없다.**
