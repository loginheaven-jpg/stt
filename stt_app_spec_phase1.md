# 받아쓰기 로컬 시스템 — Phase 1 작업지시서

> 대상 : Claude Code
> 기준 코드 : `app.py` v2026-08-13.7
> 작성일 : 2026-08-13

---

## 0. 이 문서를 읽는 법

**§6 금지 조항을 먼저 읽는다.** 그 목록은 이미 실패해 본 것들이다. 다시 밟으면 같은 시간을 다시 쓴다.

기존 `app.py`는 검증된 자산이다. 갈아엎지 않는다. 전사·화자분리·교정 로직은 그대로 두고 그 위에 대기열과 화면을 얹는 일이다.

---

## 1. 목적과 범위

### 1-1. 무엇을 만드는가

한국어 음성을 텍스트로 바꾸는 **로컬 전용 데스크톱 도구**다. 음성이 인터넷으로 나가지 않고 비용이 들지 않는다.

지금은 명령창에서 `python app.py`를 쳐야 하고, 파일을 하나씩 돌려야 하며, 무엇을 언제 돌렸는지 남지 않는다. 이것을 고친다.

### 1-2. Phase 경계

| 구분 | Phase 1 (이번) | Phase 2 (다음) |
|---|---|---|
| 엔진 | whisper_local + pyannote만 | RTZR · CLOVA · OpenAI 통합 |
| 비용 | 0원 | 유료 엔진 선택·비용 고지 |
| 배포 | 바로가기 + 설치 스크립트 | 필요 시 exe |
| 정본화 | 초안 md 산출까지 | 판정·부록 생성 |

**Phase 1에 유료 엔진을 넣지 않는다.** 별도 도구 `run_stt.py`·`stt_bench.py`는 건드리지 않고 그대로 둔다.

### 1-3. 왜 무료 전용인가

측정 결과다. 같은 음원, 같은 정답지로 잰 수치다.

| 엔진 | 화자 오귀속률 | 화자정답률 | CER | 고유명사 | 비용 |
|---|---|---|---|---|---|
| **whisper_local + pyannote** | **4.35%** | **73.3%** | **9.38%** | **100%** | **0원** |
| CLOVA Speech | 14.29% | 60.0% | 11.46% | 100% | 1,200원/시간 |
| OpenAI diarize | 5.88% | 53.3% | 20.83% | 0% | 약 500원/시간 |
| RTZR sommers | 33.33% | 53.3% | 10.42% | 100% | 1,000원/시간 |

**무료 조합이 유료 셋을 모두 앞선다.** 유료 엔진을 붙일 이유가 지금은 없다.

---

## 2. 현재 자산

| 파일 | 상태 | 이번 작업에서 |
|---|---|---|
| `app.py` v2026-08-13.7 | 동작 확인 | **확장한다** |
| `stt_bench.py` | 동작 확인 | 회귀 시험에 쓴다. 수정 금지 |
| `run_stt.py` | 동작 확인 | Phase 2용. 건드리지 않는다 |
| `convert_clovanote.py` | 동작 확인 | 건드리지 않는다 |
| `sample3.txt` | 2인 코칭 정답지 | 회귀 시험 기준 |

`app.py`에서 반드시 보존할 로직이다.

- `load_env()` — 인코딩·줄바꿈·따옴표·다중항목 처리
- `load_waveform()` — pyannote에 파형 직접 전달
- `apply_speakers()` — pyannote 턴을 따르는 화자 배정
- `fix_terms()` · `write_ledger()` — 자모 거리 교정과 대장
- `port_busy()` — 중복 실행 차단
- `dia_check()` — 화자분리 사전 점검

---

## 3. 기능 요구

### 3-1. 대기열 — Phase 1의 심장

**여러 파일을 걸어두고 자리를 뜰 수 있어야 한다.**

3시간 강의를 2배속으로 돌리면 1시간 30분이다. 파일이 다섯이면 하루가 간다. 하나 끝날 때마다 사람이 와서 다음을 눌러야 한다면 실용이 아니다.

| 요구 | 내용 |
|---|---|
| 담기 | 파일 목록에서 여러 개를 골라 대기열에 넣는다 |
| 설정 | 항목마다 다른 설정을 가질 수 있다 |
| 순서 | 위아래로 옮긴다 |
| 제거 | 대기 중인 항목을 뺀다 |
| 실행 | 위에서부터 하나씩. 동시 실행 금지 |
| 중단 | 현재 항목만 중단하고 다음으로 넘어간다 |
| 전체 중지 | 현재 항목을 끝내고 멈춘다 |
| 실패 | 한 항목이 실패해도 다음을 계속한다 |
| 재시작 복원 | 앱을 껐다 켜면 대기열이 살아 있다 |

동시 실행을 금지하는 이유는 메모리다. `large-v3`와 pyannote가 함께 뜨면 수 GB를 쓴다.

### 3-2. 이력

무엇을 언제 어떤 설정으로 돌렸는지 남긴다.

| 항목 | 내용 |
|---|---|
| 기록 | 파일명 · 길이 · 시작·종료 시각 · 소요 · 배속 |
| 설정 | 모델 · 화자 수 · 민감도 · 이름·용어 · 형식 |
| 결과 | 화자 수 · 발화 수 · 글자 수 · 교정 건수 |
| 산출 | 만들어진 파일 경로 |
| 조작 | 결과 열기 · 같은 설정으로 다시 돌리기 · 항목 삭제 |

**"같은 설정으로 다시"가 실무에서 가장 자주 쓰인다.** 이름·용어를 보태서 재실행하는 일이 잦다.

### 3-3. 출력 폴더

| 요구 | 내용 |
|---|---|
| 기본값 | 마지막으로 쓴 폴더 |
| 최근 목록 | 최근 5개를 눌러 고른다 |
| 직접 입력 | 경로 입력란 |
| 음원 옆 | "음원과 같은 폴더"를 선택할 수 있다 |
| 자동 생성 | 없는 폴더는 만든다 |
| 검증 | 존재·쓰기 권한을 확인하고 안 되면 이유를 알린다 |

브라우저는 보안상 폴더 선택 대화상자를 열지 못한다. 위 넷으로 대신한다.

### 3-4. 설정 저장

| 요구 | 내용 |
|---|---|
| 마지막 설정 | 앱을 다시 켜도 유지된다 |
| 이름 붙여 저장 | "코칭 시연", "강의" 등으로 조합을 저장 |
| 불러오기 | 목록에서 골라 적용 |
| 기본 제공 | 아래 두 개를 초기값으로 넣는다 |

| 프리셋 | 모델 | 화자 | 민감도 | 형식 |
|---|---|---|---|---|
| 코칭 시연 | large-v3-turbo | 2명 | 민감 | 정본화 초안 md + 시각 포함 txt |
| 강의 받아쓰기 | large-v3-turbo | 화자분리 끔 | — | 평문 txt + 자막 srt |

### 3-5. 무창 실행과 종료

| 요구 | 내용 |
|---|---|
| 실행 | 바탕화면 아이콘 더블클릭. **검은 창이 뜨지 않는다** |
| 브라우저 | 자동으로 열린다 |
| 종료 | **화면 안 종료 버튼.** 작업 관리자 불필요 |
| 종료 확인 | 작업 중이면 "진행 중인 작업이 있습니다. 종료할까요?" |
| 중복 실행 | 이미 떠 있으면 새로 띄우지 않고 기존 화면을 연다 |

**트레이 아이콘은 만들지 않는다.** `pystray`·`pillow` 설치가 초보자 관문을 늘린다.

`start.vbs`가 창 없이 `pythonw.exe`로 앱을 띄운다. 이미 실행 중이면 브라우저만 연다.

### 3-6. 진단 화면

설치·환경 문제를 사용자가 스스로 확인한다.

| 항목 | 표시 |
|---|---|
| 버전 | 화면·제목·콘솔 |
| 파이썬 | 버전 |
| 패키지 | faster-whisper · pyannote.audio · torch 유무와 버전 |
| HF 토큰 | 있음·없음과 길이. **값은 표시하지 않는다** |
| 모델 | 내려받힌 모델 목록과 용량 |
| 기기 | CPU · GPU 여부 |
| 폴더 | 음원·출력 폴더 경로와 존재 여부 |

---

## 4. 화면 설계

### 4-1. 구조

한 페이지에 세 구역을 둔다. 탭으로 나누지 않는다. 진행 중인 작업을 보면서 다음 파일을 담을 수 있어야 한다.

```
┌──────────────────────────────────────────────────────┐
│  받아쓰기          [로컬 처리·비용 없음]   v____  [종료] │
├──────────────────────────────────────────────────────┤
│  ■ 지금 하는 일                                        │
│    (대기열이 비어 있으면 이 구역은 숨긴다)               │
│    7.2× 실시간        남은 24:31  경과 1:12:03         │
│    ▓▓▓▓▓▓▓▓░░░│░░░░░░░░░│░░░░░░░  1:12:33 / 2:58:41  │
│    [00:04] 화자1  안녕하세요                            │
│                                    [현재 중단] [전체 중지]│
├──────────────────────────────────────────────────────┤
│  ■ 대기열 (3)                                          │
│    2  강의_2교시.m4a    2:11:40  강의 받아쓰기   [▲▼✕]  │
│    3  코칭_김OO.wav       21:14  코칭 시연      [▲▼✕]  │
├──────────────────────────────────────────────────────┤
│  ■ 담기                                                │
│    음원 폴더  C:\dev\STT\audio            [새로 고침]   │
│    ☐ 강의_1교시.m4a       2:58:41                      │
│    ☑ 코칭_이OO.m4a          18:15   [변환됨]           │
│    설정 [코칭 시연 ▾]  ▸ 자세히                         │
│    출력 [C:\dev\STT\out_text ▾]                        │
│                                    [대기열에 담기]      │
├──────────────────────────────────────────────────────┤
│  ■ 최근 기록                        [진단]              │
│    08-13 14:22  이승은코치.m4a  18:15  2.1×  화자 2명   │
│                          [열기] [같은 설정으로] [삭제]   │
└──────────────────────────────────────────────────────┘
```

### 4-2. 상세 설정

`▸ 자세히`를 펼치면 현재 `app.py`의 설정 항목이 그대로 나온다. 항목·기본값·문구를 바꾸지 않는다.

모델 · 기기 · 연산 정밀도 · 언어 · 탐색 폭 · 무음 기준 · 이름·전문용어 · 무음 건너뛰기 · 인식 실패 시 재시도 · 말버릇 살리기 · 이름·용어 교정 · 화자 분리 · 전환 민감도 · 화자 수 · 출력 형식 4종

### 4-3. 시각 원칙

기존 `app.py`의 시각 언어를 유지한다. 이 앱의 주제는 **시간**이고, 화면의 주인공은 **배속 숫자**다. 그 숫자 하나가 무료 트랙의 실용성을 판정한다.

| 요소 | 규칙 |
|---|---|
| 색 | 기존 CSS 변수 그대로. 새 색을 도입하지 않는다 |
| 글꼴 | 시각·수치는 고정폭, 본문은 시스템 산세리프 |
| 진행 막대 | 음원 시간 눈금을 유지한다. 백분율만 있는 막대로 바꾸지 않는다 |
| 문구 | 평어체. "~하세요"가 아니라 "~한다" |
| 외부 자원 | **금지.** CDN·웹폰트·아이콘 폰트를 쓰지 않는다. 오프라인에서 돌아야 한다 |

---

## 5. 데이터 구조

모든 상태는 `app.py` 폴더 아래 JSON 파일에 둔다.

```
C:\dev\STT\
  app.py  start.vbs  setup.py  env.local
  data\
    settings.json     마지막 설정 · 최근 폴더 · 프리셋
    queue.json        대기열. 재시작 시 복원
    history.json      이력. 최근 200건
  audio\              음원
  out_text\           산출 (기본 출력 폴더)
```

```jsonc
// settings.json
{
  "version": 1,
  "last": { "model": "large-v3-turbo", "device": "auto", "compute": "int8",
            "lang": "ko", "beam": 5, "silence": 500,
            "vad": true, "fallback": true, "prompt": false, "fixterms": true,
            "hotwords": "이승은, 황미혜",
            "diarize": true, "nspk": 2, "sens": "high",
            "formats": { "plain": true, "timed": true, "srt": false, "canon": true } },
  "recent_outdirs": ["C:\\dev\\STT\\out_text"],
  "presets": [ { "name": "코칭 시연", "settings": { } } ]
}

// queue.json
{ "version": 1,
  "items": [ { "id": "q_1786625294_1", "path": "C:\\...\\a.m4a",
               "outdir": "C:\\dev\\STT\\out_text",
               "state": "waiting",          // waiting·running·done·failed·cancelled
               "settings": { }, "added": "2026-08-13T14:20:11" } ] }

// history.json
{ "version": 1,
  "items": [ { "id": "h_...", "name": "이승은코치.m4a", "duration": 1095,
               "started": "...", "finished": "...", "elapsed": 521, "speed": 2.1,
               "speakers": 2, "segments": 139, "chars": 3966, "corrections": 2,
               "settings": { }, "outputs": ["C:\\...\\이승은코치_canon.md"],
               "state": "done", "message": "" } ] }
```

**쓰기는 원자적으로 한다.** 임시 파일에 쓴 뒤 `os.replace`로 바꾼다. 전사 도중 앱이 죽어도 JSON이 깨지지 않아야 한다.

**`version` 필드를 반드시 둔다.** Phase 2에서 구조가 바뀔 때 이전 파일을 읽을 근거가 된다.

---

## 6. 기술 제약과 금지 조항

### 6-1. 금지

이미 실패해 본 것들이다.

| 금지 | 실패 내용 |
|---|---|
| `.ps1` · `.cmd` 래퍼 생성 | 실행 정책·LF 줄바꿈·인코딩으로 세 번 막혔다. **파이썬과 `.vbs`만** |
| Flask · FastAPI 등 프레임워크 | 설치 관문을 늘린다. **표준 라이브러리 `http.server`로 충분하다** |
| `localStorage` · `sessionStorage` | 상태는 서버 JSON에 둔다 |
| CDN · 웹폰트 · 외부 아이콘 | 오프라인에서 깨진다 |
| pyannote에 **파일 경로** 전달 | `torchcodec is not available`. **`load_waveform()`으로 파형을 넘긴다** |
| 화자 구조를 **사후 평활화**로 조정 | 맞장구 "네", "네네"가 통째로 상대 발화에 흡수됐다. **pyannote 턴을 그대로 따른다** |
| `condition_on_previous_text=True` | 침묵 구간에서 앞 문장을 무한 반복하는 환각 |
| 지정하지 않은 단어 교정 | 창작이 된다. **이름·용어 칸에 적힌 것만 손댄다** |
| 교정 대장 없이 본문 수정 | 무엇을 왜 바꿨는지 남지 않으면 보정이 또 다른 왜곡이 된다 |

### 6-2. 유지

`app.py`에 이미 들어 있다. 제거하거나 약화하지 않는다.

| 항목 | 이유 |
|---|---|
| `Cache-Control: no-store` 헤더 | 브라우저가 이전 화면을 붙들어 갱신이 안 보였다 |
| 포트 점유 검사 | 윈도우에서 옛 프로세스가 살아 응답하는 사고 |
| `allow_reuse_address = (os.name != "nt")` | 위와 같은 이유 |
| 버전 표시 3곳 | 콘솔·탭 제목·화면 배지 |
| `env.local` 관대한 파싱 | BOM·CRLF·따옴표·다중항목·특수 줄바꿈 |
| 구간마다 즉시 저장 후 `flush()` | 2시간 50분에 죽어도 거기까지 남는다 |
| 2단계 구조 (전사 → 화자 분리) | 화자 분리가 실패해도 텍스트는 남는다 |
| `dia_check()` 사전 점검 | 20분 돌리고 나서 실패를 알게 되는 일을 막았다 |

### 6-3. 실행 환경

| 항목 | 값 |
|---|---|
| OS | 윈도우 10·11 우선. macOS·리눅스에서도 깨지지 않게 |
| 파이썬 | 3.10 이상 |
| 필수 패키지 | `faster-whisper` |
| 선택 패키지 | `pyannote.audio` · `torch` (화자 분리용) |
| 네트워크 | 최초 모델 다운로드 외에는 불필요 |
| 파일명 | 한글 파일명이 깨지지 않아야 한다. 모든 입출력 UTF-8 |

---

## 7. 설치 프로그램 — `setup.py`

문서로 다섯 관문을 넘게 하는 것은 무리다. 스크립트가 대신한다. **더블클릭으로 실행되어야 한다.**

| 순서 | 동작 | 실패 시 |
|---|---|---|
| 1 | 파이썬 버전 확인 (3.10+) | 다운로드 주소와 **"Add Python to PATH" 체크 안내**. 종료 |
| 2 | `pip install faster-whisper` | 오류 전문과 함께 재시도 안내 |
| 3 | 화자 분리 사용 여부를 묻는다 | 아니오면 4~6 건너뜀 |
| 4 | `pip install pyannote.audio` | 용량(약 2.5GB)을 미리 알리고 확인받는다 |
| 5 | HF 토큰 입력 | 발급 절차를 화면에 단계별로. 붙여넣기까지 대기 |
| 6 | `env.local`에 `HF_TOKEN=` 저장 | 기존 파일이 있으면 그 줄만 갱신 |
| 7 | 모델 미리 받기 | 진행률 표시. 건너뛰기 허용 |
| 8 | `audio` · `out_text` · `data` 폴더 생성 | — |
| 9 | 바탕화면 바로가기 생성 | 실패해도 `start.vbs` 위치를 알린다 |
| 10 | 자가 점검 후 요약 표시 | 항목별 통과·실패 |

**5번이 관문이다.** 토큰 발급 절차를 화면에 이렇게 안내한다.

```
화자 분리를 쓰려면 HuggingFace 토큰이 필요합니다.

  1) huggingface.co 에 가입합니다
  2) huggingface.co/pyannote/speaker-diarization-community-1 에서
     약관에 동의합니다
  3) huggingface.co/settings/tokens 에서
     "Create new token" → 종류를 Read 로 선택합니다
  4) 만들어진 hf_ 로 시작하는 값을 복사해 아래에 붙여넣습니다

토큰 (건너뛰려면 그냥 Enter) >
```

10번 요약은 이 형태다.

```
설치 결과
  통과   파이썬 3.12.3
  통과   faster-whisper 1.2.0
  통과   pyannote.audio 4.0.1
  통과   HF_TOKEN (37자)
  통과   모델 large-v3-turbo (1.6GB)
  통과   바탕화면 바로가기

  바탕화면의 "받아쓰기" 를 더블클릭하면 시작됩니다.
```

---

## 8. 설치 문서 — `설치안내.md`

**대상은 초보자다.** 명령창을 모르는 사람을 전제한다.

| 절 | 내용 |
|---|---|
| 1 | 이 프로그램이 하는 일. 3줄 |
| 2 | 준비물 — 파이썬 설치. **스크린샷 2장** |
| 3 | `setup.py` 더블클릭. 화면 안내를 따른다 |
| 4 | 첫 실행 — 음원을 `audio` 폴더에 넣는다 |
| 5 | 화면 사용법 — 담기·대기열·결과 |
| 6 | 문제가 생기면 — 진단 화면 보는 법 |
| 7 | 자주 막히는 곳 |

7절에 넣을 항목이다. 전부 실제로 겪은 것이다.

| 증상 | 원인 | 조치 |
|---|---|---|
| `python은 명령이 아닙니다` | PATH 미등록 | 파이썬 재설치, 체크박스 확인 |
| 화면이 안 열린다 | 브라우저 자동 실행 실패 | `http://127.0.0.1:8765` 직접 입력 |
| 바꿨는데 화면이 그대로 | **이전 프로세스가 살아 있다** | 종료 버튼 또는 `taskkill /F /IM pythonw.exe` |
| 화자 분리가 안 된다 | 토큰 없음·약관 미동의 | 진단 화면 확인 |
| 이름이 계속 틀린다 | **이름·용어 칸이 비었다** | 회색 글씨는 예시일 뿐이다 |
| 너무 느리다 | CPU에서 large-v3 사용 | 모델을 turbo로 |

스크린샷은 두 장만 만든다. 파이썬 다운로드 페이지, 그리고 **"Add Python to PATH" 체크박스**다. 나머지는 글로 충분하다.

---

## 9. 감수 기준

**"잘 만들어라"는 감수할 수 없다.** 아래는 통과·실패가 갈리는 조건이다.

### 9-1. 회귀 — 가장 중요하다

기존 품질이 떨어지지 않았음을 숫자로 확인한다.

```
python stt_bench.py sample3.txt "out_text/이승은코치_canon.md" \
       --nouns 이승은 황미혜 이대표님 --detail
```

| 지표 | 기준 | 판정 |
|---|---|---|
| 화자 오귀속률 | 4.35% 이하 | 초과 시 실패 |
| 화자정답률 | 73.3% 이상 | 미만 시 실패 |
| CER | 9.38% 이하 | 초과 시 실패 |
| 고유명사 재현율 | 100% | 미만 시 실패 |
| 삽입 어절 | 0 | 1 이상이면 실패 |

같은 음원, 같은 설정(모델 `large-v3-turbo`, 화자 2명, 민감도 민감, 이름·용어 `이승은, 황미혜, 이대표님, 스쿼트, 에스컬레이터, 헬스클럽, 대중교통`)으로 잰다.

### 9-2. 기능

| 항목 | 통과 조건 |
|---|---|
| 대기열 | 3개를 담고 자리를 떠도 순서대로 끝난다 |
| 대기열 복원 | 앱을 껐다 켜도 남은 항목이 살아 있다 |
| 실패 격리 | 2번이 실패해도 3번이 실행된다 |
| 중단 복구 | 2시간 작업을 90분에 끊어도 그때까지 텍스트가 남는다 |
| 출력 폴더 | 지정한 폴더에 저장되고 최근 목록에 오른다 |
| 설정 저장 | 앱을 다시 켜도 마지막 설정이 유지된다 |
| 프리셋 | 저장·불러오기가 된다 |
| 무창 실행 | 바로가기 더블클릭. **검은 창이 뜨지 않는다** |
| 종료 | 화면 버튼으로 끝난다. 작업 관리자 불필요 |
| 중복 실행 | 두 번 눌러도 하나만 돈다 |
| 한글 파일명 | 깨지지 않는다 |
| 이력 | 배속·화자 수·교정 건수가 기록된다 |
| 진단 | 토큰 없이 실행하면 진단 화면이 원인을 짚는다 |

### 9-3. 설치

**깨끗한 PC 또는 가상머신에서 시험한다.** 개발한 PC에서의 성공은 검증이 아니다.

| 항목 | 통과 조건 |
|---|---|
| 소요 | 문서만 보고 **30분 안에 첫 전사 완료** |
| 개입 | 명령창을 직접 치는 일이 없다 (파이썬 설치 제외) |
| 실패 안내 | 각 단계 실패 시 무엇을 해야 하는지 화면에 나온다 |

---

## 10. 작업 순서

단계마다 감수를 받고 다음으로 간다. **한 번에 전부 만들지 않는다.**

| 단계 | 산출 | 인수 지점 |
|---|---|---|
| 1 | 대기열·이력·설정 저장 (백엔드) | §9-1 회귀 통과. 화면은 기존 그대로여도 된다 |
| 2 | 화면 재구성 | §9-2 기능 항목 통과 |
| 3 | `start.vbs` · 종료 · 진단 | 무창 실행·종료·중복 실행 통과 |
| 4 | `setup.py` · 설치 문서 | §9-3 깨끗한 PC 시험 통과 |

**1단계에서 화면을 건드리지 않는다.** 백엔드 구조를 바꾸면서 화면까지 바꾸면 회귀 원인을 못 찾는다.

각 단계 끝에 이것을 보고한다.

- 바뀐 파일과 이유
- §9 기준 중 확인한 항목과 결과
- 판단이 갈렸던 지점과 선택한 근거
- 다음 단계로 넘어가기 전 확인이 필요한 사항

---

## 부록. 이번 결정의 근거

**하나, `large-v3` 대신 `large-v3-turbo`를 기본으로 둔다.** 공개 리더보드 CER 차이가 0.106 대 0.109로 미미한데 속도는 3배 이상이다. 실측에서도 결과 차이가 크지 않았다.

**둘, 트레이 아이콘을 만들지 않는다.** `pystray`·`pillow` 설치가 초보자 관문을 하나 늘린다. 화면 안 종료 버튼으로 같은 목적을 이룬다.

**셋, 전환 민감도 기본값은 `민감`이다.** 짧은 맞장구를 잘못 남기는 것이 잘못 지우는 것보다 낫다. 남은 것은 판단으로 걸러낼 수 있지만 지워진 것은 되살릴 수 없다.

**넷, 화자 구조는 pyannote가 정한다.** 어절 단위로 화자를 조회해 사후에 다듬는 방식을 썼다가 "네 반갑습니다" 같은 실제 발화가 상대 블록에 흡수됐다. 구조 판단은 pyannote의 일이고, Whisper는 텍스트를 맡는다.
