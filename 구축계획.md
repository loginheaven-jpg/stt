# 받아쓰기 로컬 시스템 — Phase 1 구축계획

> 근거 : `stt_app_spec_phase1.md` · `app.py` v2026-08-13.7
> 작성일 : 2026-08-13
> 성격 : 지시서를 실행 가능한 작업 단위로 옮긴 문서다. 지시서와 어긋나는 곳은 §2에 이유를 적었다.

---

## 0. 현재 상태 (2026-08-14 갱신)

`stt_app_spec_phase1_addendum.md`가 이 계획의 여섯 지적을 모두 받아들였고 **0단계 신설을 승인했다.** 보완서가 원 지시서보다 우선한다.

**0단계는 끝났다.** 결과는 [baseline/README.md](baseline/README.md)와 [baseline/baseline.json](baseline/baseline.json)에 있다. §6-1 로깅도 `app.py` v2026-08-13.8에 들어갔다.

아래 §3의 0단계 설계는 보완서 §4가 대체했다. 회귀 시험은 R1(정확도)·R2(구조) 둘이고, 정답지에 시각을 넣는 방식은 **채택하지 않았다** — 편향이 생기고, `sample3.wav`를 쓰면 애초에 절단이 필요 없다.

1단계부터가 남았다. 보완서 §6이 1단계에 요구를 넷 더했다 — 모델 캐시 재사용 · 중단 작업의 재시작 처리 · 잠금 순서 고정 · `/open` 허용 목록.

---

## 1. 착수 전에 확인한 것

계획을 세우기 전에 지시서의 전제가 지금 저장소에서 성립하는지 실제로 돌려 봤다. **다섯 가지가 어긋난다.** 이것을 모르고 1단계에 들어가면 인수 자체가 불가능하다.

### 1-1. §9-1 회귀 명령이 성립하지 않는다 — 가장 크다

지시서에 적힌 명령을 그대로 돌린 결과다.

```
python stt_bench.py sample3.txt "out_text/이승은코치_canon.md" --nouns 이승은 황미혜 이대표님 --detail
```

| 지표 | 지시서 기준 | 실측 |
|---|---|---|
| CER | 9.38% 이하 | **3868.75%** |
| 화자정답률 | 73.3% 이상 | **43.33%** |
| 고유명사 | 100% | **33.33%** |
| 삽입 어절 | 0 | **1258** |

원인은 셋이고 서로 독립이다.

**하나, 정답지에 시각이 없다.** `sample3.txt`는 `화자1: 안녕하세요…` 형식이다. `stt_bench.py`는 정답지에 시각이 없으면 `--full`로 넘어가 구간을 자르지 않는다. 30어절짜리 정답지에 18분 전사문 전체를 맞대는 셈이다. 스크래치패드에서 정답지에 시각을 붙여 다시 재니 구간 절단(00:00~00:17)이 정상 동작했고 CER이 65.62%로 내려왔다. 절단 문제인 것이 확인됐다.

**둘, 지정된 산출물이 기준 설정으로 만들어지지 않았다.** `out_text/이승은코치_canon.md`에는 `이승님입니다`, `황미애입니다`가 그대로 있다. §9-1이 요구하는 이름·용어(`이승은, 황미혜, …`)와 교정 기능을 켜고 만든 파일이 아니다. 고유명사 재현율이 100%가 될 수 없다.

**셋, 정답지와 산출물의 구간이 어긋난다.** 정답지는 18분 파일의 앞 17초만 덮는데 §9-1은 18분 전사문 전체를 hyp으로 지정했다. 시각을 붙여 절단해도 경계 여유 8초 안에 든 발화가 삽입 12어절로 잡혔다.

> **정정.** 이 항목을 처음 쓸 때 "어떤 산출물을 갖다 대도 삽입 0은 나오지 않는다"고 적었다. 틀렸다. `이승은코치.m4a` 산출물에 대해서만 맞는 말이었다. 회귀 음원을 `sample3.wav`로 바꾸면 정답지가 음원 전체를 덮으므로 절단이 필요 없고, 실측에서 **삽입 0**이 나왔다. 보완서 §4-1의 판단이 옳았다.

지시서 §1-3 표의 수치(오귀속 4.35% · 정답률 73.3% · CER 9.38%)를 만든 산출물은 저장소에 남아 있지 않다. `out_sample3/`에는 `run_stt.py`가 만든 엔진별 파일만 있고, 그중 `whisper_local`은 화자 분리를 하지 않는 트랙이라 화자 지표를 낼 수 없다.

**결론 — 회귀 기준선을 먼저 복원해야 한다. 이것을 0단계로 신설한다.**

### 1-2. 채점기가 콘솔 인코딩에서 죽는다

```
UnicodeEncodeError: 'cp949' codec can't encode character '\u2014'
```

출력을 파이프나 파일로 넘기면 파이썬이 콘솔 API 대신 로케일 인코딩(cp949)을 쓴다. `—`(U+2014)가 여기서 걸린다. 대화형 콘솔에서는 드러나지 않다가 로그를 남기려는 순간 터진다. `stt_bench.py`는 수정 금지 자산이므로 **파일을 고치지 않고 `PYTHONIOENCODING=utf-8`로 고정한다.** 같은 이유로 `app.py`도 출력을 리다이렉트하는 순간 같은 자리에서 죽는다(§5-1).

### 1-3. 무창 실행에서는 모든 출력이 사라진다

`.vbs`로 `pythonw.exe`를 창 없이 띄워 확인했다. `sys.stdout is None`이다. `print()`는 예외를 내지 않고 **조용히 아무것도 하지 않는다.** 죽지 않는 대신 더 나쁘다 — 화자 분리 실패 사유, 모델 적재 오류, 스레드 예외가 전부 흔적 없이 사라진다.

`app.py`는 `sys`를 import하지 않고 `print()`만 쓰므로 크래시는 없다. 그러나 3단계에서 무창 실행을 켜는 순간 진단 수단이 0이 된다. **로그 리다이렉트가 무창 실행의 선행 조건이다.**

### 1-4. `.vbs`는 UTF-8로 저장하면 한글 경로에서 깨진다

BOM 없는 UTF-8로 쓴 `.vbs`를 cscript가 ANSI로 읽어 `파일 이름, 디렉터리 이름 또는 볼륨 레이블 구문이 잘못되었습니다` 로 실패했다. 이 PC의 사용자 폴더는 `C:\Users\<한글 계정명>\…`다. 바탕화면 바로가기와 설치 스크립트가 반드시 이 경로를 지난다.

**규칙 둘.** `.vbs`에는 한글 문자열을 넣지 않고 경로를 박지 않는다. 자기 위치(`WScript.ScriptFullName`)에서 폴더를 얻는다. 한글이 불가피하면 UTF-16LE로 저장한다.

### 1-5. `OUTDIR`이 모듈 전역이고 경로 검사가 헐겁다

출력 폴더를 항목마다 다르게 두려면(§3-3) 전역을 걷어내야 한다. `write_outputs()` · `write_ledger()` · `transcribe()` · `list_audio()` · `/open` 다섯 곳이 `OUTDIR`을 직접 본다.

같이 눈에 띈 것이 있다. [app.py:1198](app.py#L1198)의 경로 검사가 `startswith`다.

```python
if os.path.isfile(p) and os.path.abspath(p).startswith(OUTDIR):
```

`C:\dev\STT\out_text_비밀\x.txt`가 통과한다. 어차피 이 줄을 고쳐야 하니 그때 담기 방식으로 바꾼다.

---

## 2. 지시서와 달라지는 점 — 전부 승인됐다

| 항목 | 원 지시서 | 확정 | 근거 |
|---|---|---|---|
| 단계 수 | 4단계 | **0단계 신설 후 5단계** | 보완서 §1 승인 |
| §9-1 수치 | 4.35% · 73.3% · 9.38% · 100% · 0 | **폐기.** R1·R2 실측치로 대체 | 보완서 §4 |
| 회귀 음원 | `이승은코치.m4a` | **`sample3.wav`** (R1) · `이승은코치.m4a`는 구조 회귀(R2)로 | 보완서 §4-1·§4-3 |
| 정답지 | `sample3.txt` | **`sample3.txt` 그대로.** 시각을 넣지 않는다 | 시각을 넣으면 그 출처 엔진에 유리해진다 |
| `stt_bench.py` | 수정 금지 | 채점 로직만 동결. 인코딩 수정 허용 | 보완서 §5 |

정답지에 시각을 넣자던 이 계획의 원안은 채택되지 않았다. 대신 음원 전체를 덮는 짧은 정답지를 쓴다. 더 나은 판단이었다 — 편향이 없고 절단 자체가 사라진다.

---

## 3. 0단계 — 회귀 기준선 복원과 동결  ✔ 완료

**결과는 [baseline/README.md](baseline/README.md)에 있다.** 여기에는 무엇을 왜 그렇게 했는지만 남긴다.

| 순서 | 한 일 | 결과 |
|---|---|---|
| 1 | `sample3.txt`가 음원 전 구간을 덮는지 확인 | **고치지 않았다.** 5개 엔진 출력이 모두 같은 발화에서 끝나고, 에너지 분석에서도 마지막 발성이 음원 끝과 일치했다 |
| 2 | §6-1 로깅을 `app.py`에 넣는다 | v2026-08-13.8. 무창 실행에서 스레드 역추적까지 `data/app.log`에 남는 것을 확인했다 |
| 3 | R1 — `sample3.wav` 3회 전사·채점 | 정확도 지표 편차 **0**. 세 번이 완전히 같았다 |
| 4 | R2 — `이승은코치.m4a` 1회 전사 | 구조 지표 6종 기록 |
| 5 | `baseline/` 동결 | 산출물 4 + `baseline.json` + 재현 절차 |

판단이 갈렸던 곳 셋이다.

**하나, 정답지의 앞 1.5~2.7초.** 에너지는 있는데 어느 엔진도 글자를 내지 않았다. 그 구간만 잘라 무음 필터를 끄고 돌리니 `다음 영상에서 만나요.`가 나왔다 — 종료 시각이 클립 길이를 넘는 전형적인 환각이다. 발화가 아니라고 판정하고 정답지를 그대로 뒀다.

**둘, 기기를 `auto`로 뒀다.** 이 계획의 원안은 기기를 못 박자는 것이었으나 보완서 §4-2가 `auto`를 지정했다. 실제로는 CUDA(RTX 3060)로 잡혔고, 그 사실을 `baseline.json`의 `device_actual`에 남겼다. 기기가 바뀌면 기준선을 다시 잡는다는 조건을 `README.md`에 적었다.

**셋, 결함을 안은 채 얼렸다.** `fix_terms()`가 R2에서 3건을 교정했는데 3건 모두 어절을 깨뜨렸다. 0단계는 재는 단계이지 고치는 단계가 아니므로 그대로 동결하고 `baseline.json`의 `known_defects`와 `README.md` §4에 적었다. 고치면 R1 CER과 R2 교정 건수가 바뀐다.

---

## 4. 1단계 — 대기열·이력·설정 (백엔드)

**화면을 건드리지 않는다.** 지시서 §10의 지침이고, 회귀 원인을 좁히기 위해서다. 기존 화면이 그대로 동작해야 한다.

### 4-1. 전략 — 기존 경로를 큐의 특수한 경우로 만든다

`POST /start`를 없애지 않는다. **큐에 1건을 넣고 즉시 도는 것**으로 속을 바꾼다. 기존 화면의 시작 버튼·`/status` 폴링·중단 버튼이 손대지 않아도 그대로 동작한다. 이것이 1단계에서 회귀를 지키는 열쇠다.

### 4-2. 파일 배치

`app.py` 단일 파일을 유지한다. 지시서 §5의 폴더 배치에 다른 `.py`가 없고, 파일 하나만 복사하면 되는 단순함이 이 도구의 자산이다. 약 1300줄에서 1900줄 안팎이 된다.

```
C:\dev\STT\
  app.py                       ← 확장
  data\
    settings.json   queue.json   history.json
  baseline\                    ← 0단계 산출
```

### 4-3. 상태 계층

```python
DATA = os.path.join(BASE, "data")
STATE_LOCK = threading.RLock()          # JOB의 LOCK과 별개다

def load_json(path, default):           # 깨졌으면 .bad 로 옮기고 기본값
def save_json(path, obj):               # 임시 파일 → flush → fsync → os.replace
```

**`STATE_LOCK`과 `LOCK`을 분리한다.** `LOCK`은 전사 루프가 구간마다 잡는다. 여기에 디스크 쓰기를 얹으면 전사가 디스크를 기다린다.

**획득 순서는 `STATE_LOCK → LOCK → _LOG_LOCK` 하나로 고정한다.** 역순으로 잡는 경로가 하나라도 생기면 교착한다(보완서 §6-4). `_LOG_LOCK`은 잎 잠금이다 — 이것을 쥔 채 다른 잠금을 잡지 않는다. 0단계에서 넣은 로깅 블록 머리에 같은 문장을 주석으로 남겼다.

**진행률로는 저장하지 않는다.** 상태 전이(담김·시작·끝남·실패·순서 변경)에서만 쓴다. 3시간 전사가 초당 몇 번씩 JSON을 쓰면 안 된다.

JSON 구조는 지시서 §5를 그대로 쓴다. `version` 필드를 셋 다 둔다.

기본 프리셋 2종은 지시서 §3-4 표대로 넣는다.

| 프리셋 | 모델 | 화자 | 민감도 | 형식 |
|---|---|---|---|---|
| 코칭 시연 | large-v3-turbo | 2명 | 민감 | canon + timed |
| 강의 받아쓰기 | large-v3-turbo | 분리 끔 | — | plain + srt |

### 4-4. 대기열 실행기

```python
STOP_ALL = threading.Event()       # 전체 중지. 현재 항목은 끝낸다
CANCEL   = threading.Event()       # 현재 항목만 중단 (기존 그대로)

def queue_loop():                  # 앱 기동 시 daemon 스레드 하나
    while True:
        if STOP_ALL.is_set() or not (item := take_next()):
            time.sleep(0.4); continue
        CANCEL.clear()
        try:
            transcribe(item["path"], item["settings"], item["outdir"])
        except Exception as e:
            with LOCK: JOB.update(state="error", message=f"{type(e).__name__}: {e}")
        finish_item(item)          # 이력 기록 → 큐에서 제거 → 저장
```

**동시 실행 금지가 구조로 보장된다.** 실행기가 하나뿐이라 플래그 검사에 기대지 않는다. 지시서가 금지한 이유는 메모리이고, 검사보다 구조가 확실하다.

**실패 격리** — `transcribe()`가 예외를 던져도 `try`가 받고 `finish_item()`이 다음으로 넘긴다. 한 항목의 실패가 대기열을 멈추지 않는다.

**재시작 복원** — 기동 시 `queue.json`의 `running`을 **`interrupted`로 바꾸고 자동 재실행하지 않는다.** 원안은 `waiting`으로 되돌려 다시 돌리는 것이었으나 보완서 §6-3이 막았다. 부분 산출 파일이 이미 있는데 처음부터 다시 돌면 덮어쓴다. 화면이 "중단된 작업입니다. 다시 하시겠습니까?"를 묻고, 기존 산출물을 `이름_부분.txt`로 보존할지 덮어쓸지 사용자가 고른다.

**모델 캐시** — `(model, device, compute)`를 키로 `WhisperModel`과 pyannote 파이프라인을 보관한다. 다음 항목이 같은 조합이면 그대로 쓰고, 바뀌면 이전 것을 버린다. 대기열이 비고 10분이 지나면 놓는다. 지금은 항목마다 새로 만들어 5건이면 5번 적재한다. **R2의 배속 지표가 이 요구의 검증 수단이다.**

**완료 항목은 큐에서 뺀다.** `queue.json`에는 `waiting`·`running`만 남고, 끝난 것은 `history.json`으로 옮긴다. 대기열 화면이 곧 남은 일이 된다.

### 4-5. 함수 서명 변경

출력 폴더를 항목별로 두기 위한 최소 변경이다.

| 함수 | 변경 |
|---|---|
| `transcribe(path, opt)` | → `transcribe(path, opt, outdir)` |
| `write_outputs(rows, stem, want, diarized)` | → `(…, outdir)` |
| `write_ledger(entries, stem, off)` | → `(…, outdir)` |
| `list_audio()` | → `list_audio(outdirs)` — 변환됨 판정을 최근 폴더들에서 본다 |
| `/open` 경로 검사 | `startswith` → 등록된 출력 폴더 목록에 담김 검사 |

`OUTDIR`은 남긴다. 기본값이자 최초 실행 시의 출력 폴더다.

허용 뿌리는 **설정에 등록된 출력 폴더 목록**이다 — `{OUTDIR} ∪ settings.recent_outdirs`. 임의 경로를 그때그때 받지 않는다(보완서 §6-5). 검사는 이렇게 한다.

```python
def under(path, root):
    a, b = os.path.normcase(os.path.abspath(path)), os.path.normcase(os.path.abspath(root))
    return a == b or a.startswith(b + os.sep)
```

`os.path.commonpath`는 드라이브가 다르면 예외를 던지므로 위처럼 정규화한 접두 검사에 구분자를 붙여 쓴다.

### 4-6. `JOB`에 더할 키

기존 키는 하나도 바꾸지 않는다. 화면이 그대로 돌아야 한다.

| 키 | 용도 |
|---|---|
| `qid` | 지금 도는 큐 항목 id |
| `outdir` | 이 항목의 출력 폴더 |
| `corrections` | 교정 건수. 지금은 메시지 문장에만 있어 이력에 못 남긴다 |

### 4-7. 엔드포인트

1단계에서는 화면이 쓰지 않아도 된다. 다음 단계의 접합면이다.

| 메서드 | 경로 | 하는 일 |
|---|---|---|
| GET | `/queue` `/history` `/settings` | 읽기 |
| POST | `/queue/add` | `{paths[], settings, outdir}` — 여러 건을 한 번에 |
| POST | `/queue/move` | `{id, dir}` 위아래 |
| POST | `/queue/remove` | `{id}` |
| POST | `/queue/stopall` | `{on}` 전체 중지·재개 |
| POST | `/history/remove` `/history/again` | 삭제 · 같은 설정으로 다시 |
| POST | `/settings` | 마지막 설정 · 프리셋 저장 |

`/status` `/start` `/cancel` `/files` `/diacheck` `/version` `/open`은 그대로 둔다.

### 4-8. 인수

- **0단계 기준선 다섯 지표가 그대로 나온다** — 이것이 1단계의 본 시험이다
- 기존 화면에서 파일 하나를 골라 시작·중단·완료가 된다
- `/queue/add`로 3건을 넣으면 순서대로 하나씩 돈다
- 2번을 없는 경로로 넣어도 3번이 실행된다
- 앱을 껐다 켜면 남은 항목이 살아 있다
- `data\*.json`이 전사 중 강제 종료 후에도 열린다

---

## 5. 2단계 — 화면 재구성

### 5-1. 구성

지시서 §4-1의 네 구역을 한 페이지에 둔다. 탭으로 나누지 않는다. `PAGE` 문자열 하나를 유지한다.

폴링은 `/state` 하나로 합친다. 구역이 넷이라고 요청을 넷 보내지 않는다.

```
GET /state → { job, queue[], history[최근 20], settings, outdirs[], diacheck }
```

간격은 지금과 같은 900ms. 대기열이 비어 있고 아무것도 돌지 않으면 3초로 늦춘다.

### 5-2. 구역별 요점

| 구역 | 요점 |
|---|---|
| 지금 하는 일 | 기존 신호판을 그대로 옮긴다. 배속·시간 눈금 막대·꼬리 미리보기. 대기열이 비면 숨긴다 |
| 대기열 | 순서·▲▼·✕. 현재 중단 / 전체 중지 |
| 담기 | 체크박스 다중 선택. 프리셋 고르개. `▸ 자세히`는 `<details>`로 접는다 |
| 최근 기록 | 열기 · 같은 설정으로 · 삭제 |

출력 폴더는 브라우저가 폴더 대화상자를 열 수 없으므로 넷으로 대신한다 — 최근 5개 · 직접 입력 · 음원과 같은 폴더 · 없으면 생성. 쓰기 권한은 서버가 실제로 임시 파일을 만들어 확인하고 안 되면 이유를 돌려준다.

### 5-3. 지키는 것

- CSS 변수 그대로. 새 색을 만들지 않는다
- 진행 막대는 시간 눈금을 유지한다. 백분율 막대로 바꾸지 않는다
- 배속 숫자가 화면에서 가장 크다
- 외부 자원 금지. `localStorage` 금지
- `▸ 자세히` 안의 항목·기본값·문구를 바꾸지 않는다

### 5-4. 인수

지시서 §9-2 중 대기열·복원·실패 격리·중단 복구·출력 폴더·설정 저장·프리셋·한글 파일명·이력 항목. 그리고 회귀 재확인.

---

## 6. 3단계 — 무창 실행 · 종료 · 진단

### 6-1. 로그 — ✔ 0단계에서 이미 넣었다

보완서 §6-1이 이것을 0단계로 앞당겼다. 로그 없이 무창 실행을 만들면 이후 모든 실패가 흔적 없이 사라지기 때문이다. `app.py` v2026-08-13.8에 들어가 있다.

들어간 것 — `data/app.log` (5MB × 3 회전) · 콘솔이 있으면 콘솔에도 쓰는 이중 출력 · 무창 실행에서 `sys.stdout`·`sys.stderr`를 가로채 스레드 역추적까지 담기 · 리다이렉트 시 UTF-8 재설정으로 §1-2의 cp949 사고 차단 · 토큰은 `mask()`로 앞 4자와 길이까지만 · `GET /log?n=` 로 최근 줄 읽기.

3단계에 남은 것은 **진단 화면에서 최근 200줄을 보여주는 일**뿐이다(§6-5).

### 6-2. 종료

```python
SRV = None                                   # main()에서 채운다

# POST /quit → 먼저 응답하고, 별도 스레드에서 SRV.shutdown()
```

`shutdown()`은 서비스 스레드에서 부르면 잠긴다. 응답을 보낸 뒤 다른 스레드에서 부른다. 작업 중이면 화면이 먼저 묻는다 — `진행 중인 작업이 있습니다. 종료할까요?`. 여기까지의 텍스트는 이미 저장돼 있다(구간마다 `flush()`).

### 6-3. 중복 실행

`port_busy()`는 이미 있다. `main()`이 지금은 안내만 찍고 끝난다. **브라우저로 기존 화면을 열고 조용히 끝내도록 바꾼다.** 무창 실행에서는 안내문을 볼 수 없기 때문이다.

### 6-4. `start.vbs`

§1-4의 규칙을 따른다. 한글을 넣지 않고 경로를 박지 않는다.

```vbs
Set sh = CreateObject("WScript.Shell")
Set fso = CreateObject("Scripting.FileSystemObject")
here = fso.GetParentFolderName(WScript.ScriptFullName)
sh.CurrentDirectory = here
On Error Resume Next
sh.Run "pythonw.exe """ & here & "\app.py""", 0, False
If Err.Number <> 0 Then MsgBox "Python not found. Run setup.py first."
```

브라우저는 `app.py`가 연다. 이미 떠 있으면 §6-3 경로로 기존 화면이 열린다.

### 6-5. 진단 `/diag`

지시서 §3-6의 일곱 항목. 항목마다 `try`로 감싼다. 하나가 실패해도 나머지가 보여야 한다.

`torch` import는 몇 초 걸린다. **`/diag`를 부를 때만 하고 결과를 캐시한다.** 기동 시에 하지 않는다.

모델 목록은 HuggingFace 캐시(`HF_HOME` 또는 `~/.cache/huggingface/hub`)의 `models--*` 폴더를 훑어 용량을 더한다.

**HF 토큰은 있음·없음과 길이만 보인다. 값은 어디에도 찍지 않는다.**

### 6-6. 인수

바탕화면 바로가기 더블클릭에 검은 창이 뜨지 않는다 · 브라우저가 자동으로 열린다 · 화면 버튼으로 종료된다 · 두 번 눌러도 하나만 돈다 · 토큰을 지우고 실행하면 진단이 원인을 짚는다 · `data\app.log`에 기동 기록이 남는다.

---

## 7. 4단계 — `setup.py`와 설치 문서

지시서 §7의 10단계를 그대로 따른다. 구현에서 걸릴 곳만 적는다.

| 지점 | 처리 |
|---|---|
| pip 호출 | `subprocess.check_call([sys.executable, "-m", "pip", "install", …])`. `pip`를 PATH에서 찾지 않는다 |
| 바탕화면 경로 | **레지스트리에서 읽는다.** `HKCU\…\Shell Folders\Desktop`. OneDrive로 옮겨진 바탕화면이 흔하다 |
| 바로가기 생성 | 임시 `.vbs`를 만들어 `cscript`로 돌린다. 추가 패키지가 필요 없고 §6-1 금지를 지킨다 |
| 창이 닫히는 것 | 마지막에 `input()`으로 멈춘다 |
| 무창 실행 방지 | `sys.stdout is None`이면 `python.exe`로 자신을 다시 띄운다 |
| 모델 미리 받기 | `WhisperModel(...)` 한 번 생성으로 내려받는다. 건너뛰기를 허용한다 |

`설치안내.md`는 지시서 §8의 7절 구성을 따른다. 스크린샷은 두 장 — 파이썬 다운로드 페이지, `Add Python to PATH` 체크박스.

**인수는 깨끗한 PC 또는 가상머신에서 한다.** 개발한 PC에서의 성공은 검증이 아니다.

---

## 8. 판단이 갈리는 지점

결정이 필요한 것과, 내가 권하는 쪽을 적는다.

| # | 지점 | 권고 | 근거 |
|---|---|---|---|
| 1 | §9-1 수치를 0단계 실측치로 대체 | **대체한다** | 재현되지 않는 수치는 기준이 못 된다. 사람의 승인이 필요하다 |
| 2 | 연속 항목의 모델 재적재 | **같은 설정이면 재사용** | `large-v3-turbo` 적재가 항목마다 수십 초다. 5건이면 몇 분을 버린다. 설정이 바뀌면 해제 후 새로 만든다 |
| 3 | 같은 이름·다른 폴더 파일의 출력 충돌 | **덮어쓴다. 담을 때 경고만** | 기존 동작을 유지한다. 접미사를 붙이면 이력·재실행 경로가 복잡해진다 |
| 4 | `app.py` 단일 파일 유지 | **유지한다** | 지시서 §5 배치에 다른 `.py`가 없다. 복사 하나로 끝나는 단순함이 자산이다 |
| 5 | 대기열 자동 시작 | **담으면 곧바로 돈다** | 별도 실행 버튼은 "걸어두고 자리를 뜬다"는 목적에 어긋난다. 전체 중지로 멈춘다 |
| 6 | 완료 항목의 자리 | **큐에서 빼고 이력으로** | 대기열 화면이 곧 남은 일이 된다 |

---

## 9. 순서와 인수 지점

| 단계 | 산출 | 인수 |
|---|---|---|
| **0 ✔** | `baseline/` · `app.py` 로깅 | 3회 편차 0 · 기준선 문서화 — **끝났다** |
| 1 | 대기열·이력·설정 백엔드 · 모델 캐시 | **R1·R2 재현** + 대기열 3건 · 실패 격리 · 중단 작업 처리 |
| 2 | 화면 재구성 | 지시서 §9-2 기능 항목 + 회귀 재확인 |
| 3 | `start.vbs` · 종료 · 중복 실행 · 진단 화면 | 무창 실행 · 종료 · 중복 실행 · 진단 |
| 4 | `setup.py` · `설치안내.md` | 깨끗한 PC에서 30분 안에 첫 전사 |

단계마다 감수를 받고 넘어간다. 한 번에 전부 만들지 않는다.

각 단계 끝에 보고할 것 — 바뀐 파일과 이유 · 확인한 인수 항목과 결과 · 판단이 갈렸던 지점과 선택 근거 · 다음 단계 전에 확인이 필요한 사항.

---

## 10. 결정 이력

§9-1 수치 대체는 보완서가 승인했고 0단계는 끝났다. `fix_terms()` 오교정도 1단계 착수 전에 정리됐다.

**내가 낸 수정안 둘은 채택되지 않았다. 틀렸기 때문이다.**

| 어절 | 조각 | 거리 | 머리 제한 | 상한 2 |
|---|---|---|---|---|
| 사이클을 | 이클을 | 3 | 탈락 | 탈락 |
| **이승입니다** | 이승입 | 2 | **통과** | **통과** |
| 이승님입니다 | 이승님 | 3 | 통과 | **탈락** |

머리 제한만으로는 `이승입니다`를 못 잡는다 — 조각이 0번째에서 시작한다. 보고서 본문에 "어느 쪽으로도 걸러진다"고 쓴 것이 오류였고 괄호 설명과도 어긋났다. 거리 상한 2는 더 나쁘다 — 고유명사 100%를 만든 거리 3짜리 `이승님 → 이승은`을 죽인다.

`상황에서`도 내 오류다. 대장의 원문 열 실제 값은 `황이라`였고 원래 어절은 **`상황이라서`**다. 결과 문자열에서 원본을 역추정하다 틀렸다.

**채택된 것은 머리 고정 + 꼬리 검증이다.** 거리 상한은 3 그대로 둔다. 조각을 어절 머리에서만 찾고, 교정 뒤 남는 꼬리가 조사·어미 목록에 있을 때만 반영한다. 없으면 본문을 그대로 두고 대장에만 올린다. 파괴 셋이 모두 막히고 정상 교정 여섯이 모두 산다.

기준선은 v2026-08-13.10에서 다시 잡았다. 바뀐 값은 R1 고유명사(100% → 66.67%)와 R2 교정(3건 반영 → 0건 반영·1건 보류) 둘뿐이고, 구조 지표는 그대로다.
